---
phase: 19.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md
  - .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md
  - .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [SAUC-10]
must_haves:
  truths:
    - Phase 18.1 has a restorable artifact trail that proves `/seller/register` exists and is protected.
    - Phase 18.1 evidence proves profile settings remains the entry path that routes authenticated non-sellers toward seller registration.
    - Verification evidence shows seller-profile creation and seller-role activation are server-authoritative, while verification/payment onboarding remain off by default.
    - The artifact trail distinguishes the transactional seller-role activation from separate frontend profile sync work.
  artifacts:
    - path: .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md
      provides: Completion summary for the surviving Phase 18.1 implementation.
    - path: .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md
      provides: Validation contract and commands for SAUC-10 evidence.
    - path: .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md
      provides: Requirement coverage proof tying FE route, API gateway, and identity-service handler together.
  key_links:
    - from: FE/artium-web/src/pages/seller/register.tsx
      to: FE/artium-web/src/@domains/seller/views/SellerRegistrationPage.tsx
      via: Protected dynamic route render
      pattern: dynamic
    - from: FE/artium-web/src/@domains/profile/views/ProfileEditPage.tsx
      to: FE/artium-web/src/pages/seller/register.tsx
      via: Profile settings seller-registration entry path
      pattern: /seller/register
    - from: FE/artium-web/src/@shared/apis/profileApis.ts
      to: BE/apps/api-gateway/src/presentation/http/controllers/identity/seller-profiles.controller.ts
      via: POST /identity/seller-profiles
      pattern: createSellerProfile
    - from: BE/apps/api-gateway/src/presentation/http/controllers/identity/seller-profiles.controller.ts
      to: BE/apps/identity-service/src/application/commands/handlers/CreateSellerProfile.command.handler.ts
      via: Authenticated seller-profile creation path
      pattern: UserRole.SELLER
---

<objective>
Reconstruct the missing Phase 18.1 artifacts so SAUC-10 has durable summary, validation, and verification evidence.

Purpose: Remove the orphaned Phase 18.1 audit gap without redesigning seller registration.
Output: Restored 18.1 planning artifacts aligned to the surviving FE and BE implementation.
</objective>

<context>
@.planning/phases/18.1-seller-registration-and-role-activation-gap-closure/PLAN.md
@.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-RESEARCH.md
@.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-VALIDATION.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-VERIFICATION.md
@FE/artium-web/src/pages/seller/register.tsx
@FE/artium-web/src/@domains/seller/views/SellerRegistrationPage.tsx
@FE/artium-web/src/@shared/apis/profileApis.ts
@FE/artium-web/src/@domains/profile/views/ProfileEditPage.tsx
@BE/apps/api-gateway/src/presentation/http/controllers/identity/seller-profiles.controller.ts
@BE/apps/identity-service/src/application/commands/handlers/CreateSellerProfile.command.handler.ts

<interfaces>
From `FE/artium-web/src/pages/seller/register.tsx`:
```tsx
const SellerRegisterRoute: NextPageWithLayout = () => {
  const { canRenderProtected } = useRequireAuth()
  if (!canRenderProtected) return null
  return <SellerRegistrationPage />
}
```

From `FE/artium-web/src/@shared/apis/profileApis.ts`:
```ts
type CreateSellerProfileInput = {
  profileType: string
  displayName: string
  bio?: string | null
  profileImageUrl?: string | null
  coverImageUrl?: string | null
  websiteUrl?: string | null
  location?: string | null
}
```

From `BE/apps/identity-service/src/application/commands/handlers/CreateSellerProfile.command.handler.ts`:
```ts
if (!user.roles?.includes(UserRole.SELLER)) {
  await this.userRepository.update(
    userId,
    { roles: Array.from(new Set([...(user.roles ?? []), UserRole.SELLER])) },
    manager,
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the 18.1 summary and validation artifacts from surviving evidence</name>
  <files>.planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md, .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md</files>
  <action>Create `18.1-SUMMARY.md` and `18.1-VALIDATION.md` using the completed `PLAN.md` plus the current FE/BE implementation as the source of truth. The summary must document: profile-settings entry from `ProfileEditPage.tsx` into `/seller/register`, protected `/seller/register` route, seller registration form + duplicate-registration recovery, `usersApi.updateMe` plus `profileApis.createSellerProfile` on the FE, and the backend transaction that creates the seller profile and adds `UserRole.SELLER`. State explicitly that seller verification and payment onboarding remain false/incomplete by default, and do not over-claim atomicity beyond the seller-profile creation + role addition transaction. In `18.1-VALIDATION.md`, include the SAUC-10 command from `19.1-VALIDATION.md` verbatim for automated checks and add the manual route/session check for duplicate-registration recovery.</action>
  <verify>
    <automated>test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md && test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md && grep -nE "yarn build:identity|yarn build:gateway|/seller/register|isVerified|payment onboarding" .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md</automated>
  </verify>
  <done>The 18.1 summary and validation artifacts exist and accurately describe the real seller-registration implementation and proof commands.</done>
</task>

<task type="auto">
  <name>Task 2: Author Phase 18.1 verification evidence for SAUC-10</name>
  <files>.planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md</files>
  <action>Create `18.1-VERIFICATION.md` with a clear verdict, goal verification bullets, requirement coverage for SAUC-10, automated evidence, and caveats. Reference the profile-settings entry path in `ProfileEditPage.tsx`, the protected `/seller/register` route, the duplicate-registration recovery path in `SellerRegistrationPage.tsx`, the gateway `@UseGuards(JwtAuthGuard)` create route, and the identity-service transaction that adds `UserRole.SELLER` while leaving `isVerified`, `stripeOnboardingComplete`, and `paypalOnboardingComplete` unset/false by default. Use the same automated command family defined in `19.1-VALIDATION.md`; if you mention repo-wide lint caveats, keep them scoped as pre-existing and unrelated.</action>
  <verify>
    <automated>test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md && grep -nE "SAUC-10|JwtAuthGuard|UserRole\\.SELLER|isVerified|stripeOnboardingComplete|paypalOnboardingComplete" .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md</automated>
  </verify>
  <done>Phase 18.1 has verification evidence that survives re-audit and precisely matches the implemented seller-registration flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifacts → milestone audit | Missing or inaccurate evidence can misstate seller-registration security behavior. |
| frontend registration flow → backend transaction narrative | The artifact trail must distinguish browser-side profile sync from server-side role activation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-04 | Repudiation | `18.1-VERIFICATION.md` | mitigate | Cite exact FE and BE files so SAUC-10 evidence is traceable instead of summary-only. |
| T-19.1-05 | Tampering | `18.1-SUMMARY.md` | mitigate | State that only seller-profile creation plus seller-role activation are transactional; do not blur in separate FE user-profile updates. |
| T-19.1-06 | Elevation of Privilege | SAUC-10 narrative | mitigate | Document backend-authenticated seller creation and server-side role activation so the artifact trail does not imply client-authoritative role changes. |
</threat_model>

<verification>
- Automated artifact existence: `test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-SUMMARY.md && test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VALIDATION.md && test -f .planning/phases/18.1-seller-registration-and-role-activation-gap-closure/18.1-VERIFICATION.md`
- Automated implementation proof for execution: `cd FE/artium-web && npx eslint src/pages/seller/register.tsx src/@domains/seller/views/SellerRegistrationPage.tsx src/@domains/profile/views/ProfileEditPage.tsx src/@shared/apis/profileApis.ts && npx tsc --noEmit && cd ../../BE && yarn build:identity && yarn build:gateway`
</verification>

<success_criteria>
- Phase 18.1 no longer has missing summary, validation, or verification artifacts.
- The SAUC-10 evidence trail proves protected route access, duplicate recovery, and server-side role activation.
- The documents preserve the current architecture and do not claim new seller-onboarding features.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-02-SUMMARY.md`
</output>
