---
phase: 19.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FE/artium-web/src/@domains/auction/components/SellerAuctionTermsPreview.tsx
  - FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
  - FE/artium-web/src/@domains/auction/components/SellerAuctionTermsForm.tsx
autonomous: true
gap_closure: true
requirements: [SAUC-04, SAUC-05]
must_haves:
  truths:
    - The seller preview shows a visible fee and activation-cost disclosure whenever the terms step is open.
    - Policy copy states fees follow current policy, network gas is shown in MetaMask during activation, and buyer-facing disclosures are shown before bidding.
    - The Start Auction surface still validates locally and clearly says wallet/backend orchestration belongs to the next phase.
  artifacts:
    - path: FE/artium-web/src/@domains/auction/components/SellerAuctionTermsPreview.tsx
      provides: Visible fee/policy disclosure block inside the preview area.
    - path: FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
      provides: Three-card policy rail with the updated fees+lock card wording.
    - path: FE/artium-web/src/@domains/auction/components/SellerAuctionTermsForm.tsx
      provides: Start helper copy aligned to the Phase 20 boundary.
  key_links:
    - from: FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
      to: FE/artium-web/src/@domains/auction/components/SellerAuctionTermsPreview.tsx
      via: Terms-step render path
      pattern: SellerAuctionTermsPreview
    - from: FE/artium-web/src/@domains/auction/components/SellerAuctionTermsForm.tsx
      to: FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
      via: Start Auction helper and local validation callback
      pattern: onStartAttempt
---

<objective>
Apply the approved 19.1 UI-SPEC delta to the existing seller auction workspace without changing the Phase 19 architecture.

Purpose: Close the remaining SAUC-05 wording gap and preserve the explicit Phase 20 boundary for SAUC-04.
Output: Updated preview, policy rail, and helper copy in the existing seller auction FE surfaces.
</objective>

<context>
@.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-UI-SPEC.md
@.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-RESEARCH.md
@.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-VALIDATION.md
@.planning/phases/19-seller-auction-creation-workspace-and-terms-ux/19-02-SUMMARY.md
@.planning/phases/19-seller-auction-creation-workspace-and-terms-ux/19-03-SUMMARY.md
@FE/artium-web/src/@domains/auction/components/SellerAuctionTermsPreview.tsx
@FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
@FE/artium-web/src/@domains/auction/components/SellerAuctionTermsForm.tsx

<interfaces>
From `FE/artium-web/src/@domains/auction/validations/sellerAuctionTerms.schema.ts`:
```ts
export type SellerAuctionTermsFormValues = {
  reservePolicy: 'none' | 'set'
  reservePriceEth: string
  minBidIncrementEth: string
  durationPreset: SellerAuctionDurationPresetValue | 'custom'
  customDurationHours: string
  shippingDisclosure: string
  paymentDisclosure: string
  economicsLockedAcknowledged: boolean
}
```

From `FE/artium-web/src/@shared/apis/auctionApis.ts`:
```ts
export type SellerAuctionArtworkCandidate = {
  artworkId: string
  sellerId: string
  title: string
  creatorName: string
  thumbnailUrl: string | null
  status: string
  isEligible: boolean
  reasonCodes: SellerAuctionArtworkEligibilityReason[]
  recoveryActions: SellerAuctionArtworkRecoveryAction[]
}
```
 </interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the required fee and activation-cost disclosure to the preview</name>
  <files>FE/artium-web/src/@domains/auction/components/SellerAuctionTermsPreview.tsx</files>
  <action>Add a visible preview block headed `Fees and activation costs` that is rendered whenever the terms step is visible. Use the approved static copy from `19.1-UI-SPEC.md`: `Marketplace seller fees follow current policy.`, `Network gas is shown in MetaMask during activation.`, `Sepolia is a test network. Confirm wallet and network details before activation.`, and `Shipping and payment disclosures are shown to buyers before bidding.` Keep this block in the main preview/policy area, not behind an accordion, tooltip, or extra click. Preserve the existing preview card layout and do not invent numeric seller fees, calculators, wallet transaction UI, or any Phase 20 lifecycle/status surfaces.</action>
  <verify>
    <automated>cd FE/artium-web && npx eslint src/@domains/auction/components/SellerAuctionTermsPreview.tsx src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx src/@domains/auction/components/SellerAuctionTermsForm.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The preview visibly satisfies the fee/gas/buyer-disclosure copy contract for SAUC-05 while remaining pre-start only.</done>
</task>

<task type="auto">
  <name>Task 2: Align policy-rail and helper copy to the approved pre-start boundary</name>
  <files>FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx, FE/artium-web/src/@domains/auction/components/SellerAuctionTermsForm.tsx</files>
  <action>Update the three-card policy rail so one card reads `Fees and lock after activation` with body `Seller fees follow current policy. Reserve, increment, and duration lock once the auction is activated.` Update any remaining seller-workspace helper text that says `next step` to `next phase`, including the Start Auction helper, per the approved 19.1 UI-SPEC. Keep the `Start Auction` button label, placement, and current local-validation behavior exactly as-is; do not add wallet calls, backend start handoff, new routes, modals, wizard steps, tx hash UI, or seller lifecycle status UI.</action>
  <verify>
    <automated>cd FE/artium-web && npx eslint src/@domains/auction/components/SellerAuctionTermsPreview.tsx src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx src/@domains/auction/components/SellerAuctionTermsForm.tsx src/pages/artist/auctions/create.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The workspace policy rail and helper copy match the 19.1 contract and still stop before Phase 20 orchestration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser policy UI → seller decisions | Copy on the preview and policy rail influences seller expectations before activation. |
| browser form state → preview rendering | Seller-entered terms flow into preview content, while policy-only warnings must remain system-authored. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Tampering | `SellerAuctionTermsPreview.tsx` | mitigate | Keep the fee/gas/activation-policy block static and system-authored instead of deriving it from user-editable fields. |
| T-19.1-02 | Spoofing | `SellerAuctionTermsForm.tsx` helper copy | mitigate | Keep explicit `next phase` wording so the UI does not impersonate a live activation flow that does not exist yet. |
| T-19.1-03 | Repudiation | `SellerAuctionArtworkPickerPage.tsx` policy rail | mitigate | Use exact approved policy text so verification artifacts can prove the seller saw the intended constraints. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npx eslint src/@domains/auction/components/SellerAuctionTermsPreview.tsx src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx src/@domains/auction/components/SellerAuctionTermsForm.tsx src/pages/artist/auctions/create.tsx && npx tsc --noEmit`
- Manual follow-up for execution: visit `/artist/auctions/create`, open the terms step, confirm the fee/policy block is visible in the preview area, and confirm `Start Auction` stays local-only with no wallet/backend handoff.
</verification>

<success_criteria>
- The seller preview includes the approved fee and activation-cost copy.
- The policy rail retains three compact cards with the updated fees/lock wording.
- All helper text now frames auction activation as Phase 20 work, not the current step.
- No new architecture, route, or orchestration behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-seller-auction-artifact-recovery-and-policy-alignment/19.1-01-SUMMARY.md`
</output>
