---
phase: 17
plan: 03
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
files_modified:
  - FE/artium-web/src/@domains/auction/services/auctionBidWallet.ts
  - FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx
  - FE/artium-web/src/@domains/auction/components/PendingBidState.tsx
  - FE/artium-web/src/@domains/auction/components/ConfirmedBidState.tsx
  - FE/artium-web/src/@domains/auction/components/SubmittingBidState.tsx
  - FE/artium-web/src/@domains/auction/types.ts
  - FE/artium-web/src/views/LiveAuctionPage.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Bid submission uses MetaMask to submit a real `bid(orderId)` transaction to the configured escrow contract.
    - A tx hash moves the modal to pending only, not confirmed.
    - Confirmed state is shown only after refreshed backend auction state proves the bid is authoritative.
    - Conflict/failure state uses latest backend current bid and minimum next bid for retry.
  artifacts:
    - path: FE/artium-web/src/@domains/auction/services/auctionBidWallet.ts
      provides: Browser wallet bid transaction service.
    - path: FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx
      provides: Real wallet-backed bid state machine.
---

<objective>
Replace the bid modal simulation with real wallet transaction submission and backend-authoritative pending/confirmed/conflict transitions.
</objective>

<context>
@.planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-CONTEXT.md
@.planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-UI-SPEC.md
@.planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-RESEARCH.md
@FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx
@FE/artium-web/src/@domains/auth/hooks/useWalletLogin.ts
@FE/artium-web/src/@domains/auth/constants/wallet.ts
@FE/artium-web/src/@types/ethereum.d.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create wallet bid transaction service</name>
  <read_first>
    - FE/artium-web/src/@domains/auth/hooks/useWalletLogin.ts
    - FE/artium-web/src/@domains/auth/constants/wallet.ts
    - FE/artium-web/src/@domains/auth/types/wallet.ts
    - FE/artium-web/src/@types/ethereum.d.ts
    - BE/libs/blockchain/src/abi/ArtAuctionEscrow.json
  </read_first>
  <files>FE/artium-web/src/@domains/auction/services/auctionBidWallet.ts, FE/artium-web/src/@domains/auction/types.ts</files>
  <action>Create `submitAuctionBid({ auctionId, contractAddress, bidAmountEth })`. The service must read `window.ethereum`, require MetaMask/provider presence, request `eth_requestAccounts`, require Sepolia chain `0xaa36a7`, convert the decimal ETH input to wei hex without floating-point precision loss by splitting integer/fractional parts to 18 decimals, encode `bid(string)` calldata for the auction ID using the escrow ABI or a minimal local ABI signature, and call `eth_sendTransaction` with `{ from, to: contractAddress, value: weiHex, data }`. Return `{ txHash, walletAddress, chainId }`. Export `AuctionBidWalletError` or a discriminated error helper for missing wallet, wrong chain, rejected request, and invalid amount.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
  </verify>
  <acceptance_criteria>
    - `auctionBidWallet.ts` contains `eth_sendTransaction`.
    - `auctionBidWallet.ts` checks for `0xaa36a7`.
    - `auctionBidWallet.ts` exports `submitAuctionBid`.
    - Decimal ETH conversion does not use `Number(value) * 1e18`.
  </acceptance_criteria>
  <done>Bid transaction submission is isolated behind a typed wallet service.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor BidEditingModal to consume authoritative auction state</name>
  <read_first>
    - FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx
    - FE/artium-web/src/@domains/auction/components/PendingBidState.tsx
    - FE/artium-web/src/@domains/auction/components/ConfirmedBidState.tsx
    - FE/artium-web/src/@domains/auction/services/auctionBidWallet.ts
    - .planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-UI-SPEC.md
  </read_first>
  <files>FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx, FE/artium-web/src/@domains/auction/components/PendingBidState.tsx, FE/artium-web/src/@domains/auction/components/ConfirmedBidState.tsx, FE/artium-web/src/@domains/auction/components/SubmittingBidState.tsx</files>
  <action>Remove `getMockTransactionHash`, `getCompetingBid`, and the timeout effects that auto-transition pending to confirmed/failed. Add props `onRefreshLot: (auctionId: string) => Promise<AuctionBidLot>` and `contractAddress`/`onChainOrderId` via lot data. On open, call `onRefreshLot(lot.auctionId)` and update `currentBidValue`, `minimumNextBid`, `statusKey`, and `endsAt` from the returned lot. On `Place Bid`, call `submitAuctionBid`; set `submitting` during wallet handoff, set `pending` after tx hash, then poll/refresh using `onRefreshLot` until either the refreshed top bidder/current bid proves confirmation or a competing bid/stale minimum proves conflict. Keep outside click, escape, and close blocked during `submitting`. After tx hash exists, secondary copy must be `Close`, never `Cancel Transaction`. In pending copy, say the bid is awaiting backend/on-chain synchronized confirmation.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
  </verify>
  <acceptance_criteria>
    - `BidEditingModal.tsx` no longer contains `getMockTransactionHash`.
    - `BidEditingModal.tsx` no longer contains `getCompetingBid`.
    - `BidEditingModal.tsx` imports `submitAuctionBid`.
    - Failed/conflict state button copy does not contain `Cancel Transaction`.
    - Pending state copy contains `backend/on-chain synchronized confirmation` or equivalent exact phrase.
  </acceptance_criteria>
  <done>Bid modal state transitions are driven by wallet tx submission and backend refresh truth.</done>
</task>

<task type="auto">
  <name>Task 3: Connect listing refresh and modal bid lifecycle</name>
  <read_first>
    - FE/artium-web/src/views/LiveAuctionPage.tsx
    - FE/artium-web/src/@domains/auction/hooks/useAuctionLots.ts
    - FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx
    - FE/artium-web/src/@domains/auction/hooks/useAuctionRealtime.ts
  </read_first>
  <files>FE/artium-web/src/views/LiveAuctionPage.tsx, FE/artium-web/src/@domains/auction/hooks/useAuctionLots.ts</files>
  <action>Expose `refreshAuctionById(auctionId)` from `useAuctionLots` by calling `auctionApis.getAuctionById`, mapping the result to lot shape, and replacing the matching lot in local state. Pass `refreshAuctionById` into `BidEditingModal` as `onRefreshLot`. After bid pending, confirmed, or conflict state changes, ensure the listing calls `refresh` or `refreshAuctionById` so card state follows backend truth. Wire `useAuctionRealtime` to active visible auction IDs and call `refreshAuctionById` when `auctionBidUpdated`, `auctionExtended`, or `auctionStateChanged` arrives.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `useAuctionLots.ts` exports `refreshAuctionById`.
    - `LiveAuctionPage.tsx` passes `onRefreshLot` to `BidEditingModal`.
    - `LiveAuctionPage.tsx` calls `useAuctionRealtime`.
    - Card state updates only through `refresh` or `refreshAuctionById`, not by assigning committed bid values directly.
  </acceptance_criteria>
  <done>The listing and modal share one backend-authoritative refresh path.</done>
</task>

<task type="auto">
  <name>Task 4: Correct explorer links and modal copy for Sepolia</name>
  <read_first>
    - FE/artium-web/src/@domains/auction/components/PendingBidState.tsx
    - FE/artium-web/src/@domains/auction/components/ConfirmedBidState.tsx
    - FE/artium-web/src/@domains/auth/constants/wallet.ts
    - .planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-UI-SPEC.md
  </read_first>
  <files>FE/artium-web/src/@domains/auction/components/PendingBidState.tsx, FE/artium-web/src/@domains/auction/components/ConfirmedBidState.tsx, FE/artium-web/src/@domains/auction/components/BidEditingModal.tsx</files>
  <action>Replace hardcoded `https://etherscan.io/tx/` links with Sepolia explorer links from `WALLET_TARGET_CHAIN.blockExplorerUrl` or `https://sepolia.etherscan.io`. Ensure pending copy distinguishes transaction submission from final auction truth. Ensure before-submission secondary action says `Back to Auction`; after tx hash exists it says `Close`. Remove transaction cancellation language from all modal close buttons and helper copy.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `PendingBidState.tsx` does not contain `https://etherscan.io/tx`.
    - `ConfirmedBidState.tsx` does not contain `https://etherscan.io/tx`.
    - `BidEditingModal.tsx` contains `Back to Auction`.
    - `BidEditingModal.tsx` contains `Close` for post-submission secondary action.
    - `BidEditingModal.tsx` does not contain `Cancel Transaction`.
  </acceptance_criteria>
  <done>Modal copy and transaction links match Sepolia and the approved UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser to MetaMask provider | User input becomes a signed wallet transaction |
| wallet tx hash to modal state | Local transaction submission becomes pending UI state |
| backend auction DTO to confirmed UI state | Backend/on-chain synchronized state determines final modal truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | T | `auctionBidWallet.ts` | mitigate | Require Sepolia `0xaa36a7`, configured escrow contract address, and exact wei conversion before `eth_sendTransaction`. |
| T-17-08 | R | `BidEditingModal.tsx` | mitigate | Preserve tx hash in pending/confirmed UI and link to Sepolia explorer. |
| T-17-09 | T | `BidEditingModal.tsx` | mitigate | Confirmed state requires refreshed backend auction DTO matching submitted wallet/amount; local tx hash alone is insufficient. |
| T-17-10 | D | modal polling | mitigate | Use bounded polling interval and stop polling on close, confirmed, failed/conflict, or unmount. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npx tsc --noemit && npm run lint`
- Manual: submit a valid bid with MetaMask on Sepolia and capture tx hash.
- Manual: confirm modal remains pending until backend refresh shows the submitted bid as authoritative.
- Manual: simulate a competing higher bid and confirm retry uses updated backend minimum.
- Manual: close modal after tx hash and confirm copy says `Close`, not transaction cancellation.
</verification>

<success_criteria>
- Bid modal no longer uses mock tx hashes or timed mock outcomes.
- Wallet submission is real and Sepolia-gated.
- Backend/on-chain synchronized auction DTOs are the only source of confirmed bid truth.
- Listing and modal remain visually aligned with the approved UI-SPEC.
</success_criteria>

<output>
After completion, create `.planning/phases/17-auction-frontend-integration-with-blockchain-backed-backend-/17-03-SUMMARY.md`.
</output>

