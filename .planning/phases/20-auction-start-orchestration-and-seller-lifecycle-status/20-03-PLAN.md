---
phase: 20
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts
  - BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts
  - BE/apps/orders-service/src/domain/entities/orders.entity.ts
  - BE/apps/orders-service/src/infrastructure/repositories/order.repository.ts
  - BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts
  - BE/apps/artwork-service/src/application/commands/handlers/MarkArtworkInAuction.command.handler.ts
  - BE/libs/common/src/dtos/auctions/auction-read.dto.ts
autonomous: true
requirements: [SAUC-08]
must_haves:
  truths:
    - `AuctionStarted` convergence must promote a seller attempt into authoritative order/artwork state instead of relying on FE optimism.
    - Public `/auctions` reads must remain active-only and must not leak seller-only pending/failed/retryable attempts.
    - Artwork and order projections must carry enough linkage for public and seller surfaces to render real auction data rather than blanks or placeholders.
  artifacts:
    - path: BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts
      provides: Event-driven promotion from attempt state to active authoritative projection.
    - path: BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts
      provides: Public active-only auction filtering and read-model convergence.
    - path: BE/apps/artwork-service/src/application/commands/handlers/MarkArtworkInAuction.command.handler.ts
      provides: Artwork-side status and on-chain ID projection updates.
  key_links:
    - from: BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts
      to: BE/apps/orders-service/src/domain/entities/auction-start-attempt.entity.ts
      via: Promote persisted pending attempt when blockchain event confirms activation
      pattern: AuctionStarted
    - from: BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts
      to: BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts
      via: Persist artwork `IN_AUCTION` / `onChainAuctionId` after authoritative activation
      pattern: onChainAuctionId
    - from: BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts
      to: BE/apps/orders-service/src/infrastructure/repositories/order.repository.ts
      via: Exclude seller-only non-active states from public auction reads
      pattern: active|auction_active
---

<objective>
Converge seller auction starts into authoritative on-chain/off-chain state so public and downstream reads reflect only active auctions with real artwork linkage.

Purpose: Close the gap between seller start attempts and the existing public/order/artwork projections without leaking pre-activation state.
Output: Blockchain-event-driven convergence into orders/artwork data plus public active-only filtering.
</objective>

<context>
@.planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-RESEARCH.md
@.planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-PATTERNS.md
@.planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-VALIDATION.md
@BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts
@BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts
@BE/apps/orders-service/src/domain/entities/orders.entity.ts
@BE/apps/orders-service/src/infrastructure/repositories/order.repository.ts
@BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts
@BE/libs/blockchain/src/services/blockchain-event-listener.service.ts
@BE/libs/outbox/src/outbox.service.ts

<interfaces>
From `BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts`:
```ts
async handleAuctionStarted(message: AuctionStartedEventPayload) {
  ...
}
```

From `BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts`:
```ts
const orders = await this.orderRepository.findBlockchainPaymentOrders(...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote `AuctionStarted` blockchain events into authoritative order and artwork projections</name>
  <files>BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts, BE/apps/orders-service/src/domain/entities/orders.entity.ts, BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts, BE/apps/artwork-service/src/application/commands/handlers/MarkArtworkInAuction.command.handler.ts</files>
  <read_first>BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts, BE/libs/blockchain/src/services/blockchain-event-listener.service.ts, BE/libs/outbox/src/outbox.service.ts, BE/apps/orders-service/src/domain/entities/orders.entity.ts, BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts, BE/smart-contracts/contracts/ArtAuctionEscrow.sol, .planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-RESEARCH.md</read_first>
  <action>Extend the existing blockchain event path so `AuctionStarted` looks up the canonical seller start attempt by order ID, marks that attempt active, persists tx/seller/on-chain reference data, and writes authoritative order/artwork projection state. Ensure the promoted order side carries enough artwork linkage to support seller/public views, and update artwork data to `IN_AUCTION` with the on-chain auction ID only after authoritative activation is confirmed. Reuse the existing blockchain-listener and outbox patterns; do not invent a second chain-polling path.</action>
  <acceptance_criteria>
    - `AuctionStarted` can promote a persisted seller start attempt into active authoritative data exactly once.
    - Artwork projection updates occur only after authoritative chain confirmation and include `onChainAuctionId` plus active auction status.
    - The event path remains idempotent under duplicate blockchain-event delivery.
  </acceptance_criteria>
  <verify>
    <automated>cd BE && npx jest apps/orders-service/src/application/event-handlers/BlockchainEventHandler.start.spec.ts apps/artwork-service/src/application/commands/handlers/MarkArtworkInAuction.command.handler.spec.ts --runInBand</automated>
  </verify>
  <done>Authoritative blockchain convergence updates seller attempts, orders, and artworks in one durable promotion path.</done>
</task>

<task type="auto">
  <name>Task 2: Keep public auction reads active-only and backed by converged projection data</name>
  <files>BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts, BE/apps/orders-service/src/infrastructure/repositories/order.repository.ts, BE/libs/common/src/dtos/auctions/auction-read.dto.ts</files>
  <read_first>BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts, BE/apps/orders-service/src/infrastructure/repositories/order.repository.ts, BE/libs/common/src/dtos/auctions/auction-read.dto.ts, BE/apps/orders-service/src/application/queries/handlers/GetAuctionById.query.handler.ts, .planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-UI-SPEC.md</read_first>
  <action>Adjust public auction query/repository behavior so only active authoritative auctions appear in `GET /auctions`. Exclude seller-only pending, failed, and retryable attempts from public reads, and prefer converged order/artwork data that contains real titles/images/auction references rather than blank placeholder rows. Update the public auction DTOs as needed to carry the authoritative fields exposed after convergence, while preserving existing public API shape where possible.</action>
  <acceptance_criteria>
    - Public auction reads ignore non-active seller start attempts.
    - Public DTOs are backed by converged order/artwork state rather than FE-only assumptions or incomplete placeholder rows.
    - Query filtering works without breaking existing active-auction listing behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd BE && npx jest apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.spec.ts apps/orders-service/src/application/event-handlers/BlockchainEventHandler.start.spec.ts --runInBand</automated>
  </verify>
  <done>Public `/auctions` reflects only authoritative active auctions and never leaks pending or failed seller-only start state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blockchain event stream -> order/artwork projections | Duplicate or partial convergence can create stale or inconsistent public auction reads. |
| seller-only attempt state -> public auction APIs | Non-active states must stay private to seller lifecycle surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | Tampering | blockchain convergence | mitigate | Promote only persisted canonical attempts and keep event handling idempotent under duplicate delivery. |
| T-20-10 | Information Disclosure | public auction queries | mitigate | Filter non-active seller start states from public repository/query paths. |
| T-20-11 | Repudiation | authoritative activation timing | mitigate | Write artwork/order active state only after `AuctionStarted` confirmation rather than FE submit time. |
</threat_model>

<verification>
- Automated: `cd BE && npx jest apps/orders-service/src/application/event-handlers/BlockchainEventHandler.start.spec.ts apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.spec.ts apps/artwork-service/src/application/commands/handlers/MarkArtworkInAuction.command.handler.spec.ts --runInBand`
- Structure check: `grep -nE "AuctionStarted|onChainAuctionId|IN_AUCTION|active" BE/apps/orders-service/src/application/event-handlers/blockchain-event.handler.ts BE/apps/orders-service/src/application/queries/handlers/GetAuctions.query.handler.ts`
</verification>

<success_criteria>
- `AuctionStarted` drives authoritative order/artwork promotion from a persisted seller start attempt.
- Public `GET /auctions` includes only active authoritative auctions.
- Artwork and order projections carry enough linkage for downstream seller/public surfaces to render real auction data.
</success_criteria>

<output>
After completion, create `.planning/phases/20-auction-start-orchestration-and-seller-lifecycle-status/20-03-SUMMARY.md`
</output>
