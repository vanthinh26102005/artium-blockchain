---
phase: 07-checkout-payment-feedback-success-error-ui-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
  - FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
  - FE/artium-web/src/@domains/checkout/utils/checkoutSuccessState.ts
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - Buyer stays on `/checkout/[artworkId]` and sees an inline step-3 success or processing screen after Pay Now succeeds.
    - Refreshing the completed checkout route re-hydrates the inline success state instead of bouncing to `/discover`.
    - Continue Shopping clears stored checkout success state and lands on `/discover` without any checkout-success query param.
  artifacts:
    - path: FE/artium-web/src/@domains/checkout/utils/checkoutSuccessState.ts
      provides: Browser-scoped persistence helpers for checkout success display state.
      exports:
        - loadCheckoutSuccessState
        - saveCheckoutSuccessState
        - clearCheckoutSuccessState
    - path: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      provides: Success-state hydration, inline rendering, and no-redirect completion flow.
    - path: FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
      provides: Inline confirmation UI with success and wallet-processing variants plus CTA.
  key_links:
    - from: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      to: FE/artium-web/src/@domains/checkout/utils/checkoutSuccessState.ts
      via: success persistence on payment completion and hydration on mount
      pattern: saveCheckoutSuccessState|loadCheckoutSuccessState|clearCheckoutSuccessState
    - from: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      to: FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
      via: paymentResult props
      pattern: "<CheckoutSuccessScreen"
    - from: FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
      to: /discover
      via: onContinueShopping callback
      pattern: onContinueShopping
---

<objective>
Keep successful checkout completion inline on the existing checkout route, persist that display state across refresh/back-entry, and make the success CTA clear the stored state before navigating away.

Purpose: Deliver UX-01 without reintroducing redirects or query-param-driven success handling.
Output: A persisted checkout success-state helper plus updated checkout view/success screen wiring.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-CONTEXT.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-RESEARCH.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-PATTERNS.md
@FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
@FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
@FE/artium-web/src/@domains/checkout/types/buyerCheckoutTypes.ts

<decision_map>
D-01: Show checkout success inline after payment completion instead of redirecting to `/discover`.
D-02: Use a dedicated success component rendered from the checkout page view when payment completes.
D-03: Continue Shopping must navigate to `/discover` without carrying checkout success query state.
D-04: Wallet payments may show a processing-oriented success state when blockchain confirmation is still pending.
</decision_map>

<interfaces>
From `FE/artium-web/src/@domains/checkout/types/buyerCheckoutTypes.ts`:
```ts
export type ArtworkForCheckout = {
  id: string
  title: string
  artistName: string
  artistId?: string
  price: number
  priceLabel: string
  coverUrl: string
  medium?: string
  dimensions?: string
}
```

From `FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx`:
```ts
type CheckoutSuccessScreenProps = {
  orderNumber: string
  artwork: ArtworkForCheckout
  totalPaid: number
  paymentMethod: 'card' | 'wallet'
  isProcessing: boolean
  onContinueShopping: () => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a persisted checkout-success state contract</name>
  <read_first>
    - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
    - FE/artium-web/src/@domains/checkout/types/buyerCheckoutTypes.ts
    - .planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-RESEARCH.md
  </read_first>
  <files>FE/artium-web/src/@domains/checkout/utils/checkoutSuccessState.ts</files>
  <action>Create a small display-only browser persistence utility for the inline success state per D-01/D-03. Export a typed payload that carries the minimum UI data already available at payment completion (`artworkId`, `orderNumber`, `paymentMethod`, `isProcessing`, `totalPaid`) plus `load/save/clear` helpers keyed by artwork ID. Use `sessionStorage` only when `window` exists, return `null` on malformed or absent data, and do not treat stored data as a payment authority or trigger any API calls from the helper.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
  </verify>
  <acceptance_criteria>
    - The new helper exports typed load/save/clear functions.
    - Loading invalid JSON or a mismatched artwork ID returns `null` instead of throwing.
    - The helper is safe during SSR-disabled first render by guarding access to `window.sessionStorage`.
  </acceptance_criteria>
  <done>Checkout success persistence is defined behind one reusable contract and ready for the page view to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Hydrate and render the inline success screen from persisted state</name>
  <read_first>
    - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
    - FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx
    - FE/artium-web/src/@domains/quick-sell/components/checkout/QuickSellPaidState.tsx
    - FE/artium-web/src/@domains/checkout/utils/checkoutSuccessState.ts
  </read_first>
  <files>FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx, FE/artium-web/src/@domains/checkout/components/CheckoutSuccessScreen.tsx</files>
  <action>Update the checkout view to consume the new success-state helper per D-01 through D-04: remove the `?status=success` redirect/replace behavior, hydrate `paymentResult` from persisted state on mount for the current artwork, persist the success payload immediately after card or wallet success, and clear it when the buyer starts a new payment attempt or clicks Continue Shopping. Keep wallet payments on the processing-oriented variant when `isProcessing` is true, and keep the success UI inline on `/checkout/[artworkId]` with the current artwork summary plus existing “What happens next” structure rather than introducing a new route or redesign.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Successful card and wallet submissions render `CheckoutSuccessScreen` without routing to `/discover`.
    - Refreshing the completed checkout route restores the inline success or processing screen from persisted state.
    - Continue Shopping clears the persisted success payload and navigates to `/discover` with no checkout-success query param.
  </acceptance_criteria>
  <done>The checkout route owns success rendering end-to-end and no longer depends on transient query-state or immediate redirect behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser storage → checkout view | Untrusted persisted data is read back into a user-visible success screen |
| checkout view → router navigation | Client-side CTA decides when success state is cleared and where the buyer lands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | `checkoutSuccessState.ts` | mitigate | Treat stored success payload as display-only, validate shape/artwork ID on load, and never trigger payment APIs from persisted data. |
| T-07-02 | I | `CheckoutSuccessScreen.tsx` | accept | Stored payload contains order/display metadata already shown to the buyer; keep scope limited to sessionStorage and clear on Continue Shopping. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npx tsc --noemit && npm run build`
- Manual scenario 1: complete a card payment and confirm the page remains on `/checkout/[artworkId]` while rendering the inline success screen.
- Manual scenario 2: refresh the same checkout URL after success and confirm the success screen re-hydrates instead of redirecting away.
- Manual scenario 3: click Continue Shopping and confirm `/discover` loads without any checkout-success query parameter.
</verification>

<success_criteria>
- UX-01 is satisfied for both card success and wallet-processing success.
- The checkout page no longer depends on `router.query.status === 'success'` to control completion UX.
- The inline success CTA clears client-side success state before leaving checkout.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-01-SUMMARY.md`
</output>
