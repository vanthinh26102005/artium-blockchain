---
phase: 07-checkout-payment-feedback-success-error-ui-states
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
  - FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
  - FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx
  - FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts
  - .planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md
autonomous: true
requirements:
  - UX-02
  - UX-03
must_haves:
  truths:
    - Card declines show a specific inline error with a Try Again action that resets payment-step inputs without clearing step-1 contact data.
    - Network/API failures show an inline Retry action that reruns the payment attempt from step 2 without forcing the buyer to re-enter checkout data.
    - Wallet connection or transaction failures stay inline with explicit retry affordances and without expanding into Phase 8 ETH/USD correctness work.
    - Phase verification artifacts explicitly name the FE typecheck/build commands and the manual scenarios needed to prove UX-02 and UX-03.
  artifacts:
    - path: FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts
      provides: Checkout-level recovery metadata for decline vs retry semantics.
    - path: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      provides: Retry/reset handlers, error banner CTA, and preserved step-1 data across payment failures.
    - path: FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
      provides: Payment-step reset wiring without disturbing contact-form state.
    - path: FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx
      provides: Wallet retry-copy alignment and inline retry controls.
    - path: .planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md
      provides: Executable verification matrix and evidence checklist for Phase 7.
  key_links:
    - from: FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts
      to: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      via: classified recovery metadata
      pattern: classifyPaymentError|ctaLabel|recoveryAction
    - from: FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
      to: FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
      via: payment reset key / retry handler wiring
      pattern: reset|retry|key=
    - from: FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
      to: FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx
      via: wallet retry state and method-switch resets
      pattern: "<WalletPaymentSection"
---

<objective>
Make checkout payment failures explicitly recoverable, with separate reset-vs-retry semantics for card/network paths, wallet retry copy that stays inline, and validation instructions that capture the evidence required by UX-03.

Purpose: Close the remaining UX-02 gaps without expanding scope into backend changes or Phase 8 wallet-currency correctness.
Output: A recovery-aware payment error contract, updated checkout retry controls, and a refreshed Phase 7 validation matrix.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-CONTEXT.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-RESEARCH.md
@.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md
@.planning/phases/6/6-4-SUMMARY.md
@FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
@FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
@FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx
@FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts

<decision_map>
D-05: Replace generic payment errors with classified, retry-friendly inline error messages.
D-06: Card, network/API, wallet connection rejection, and wallet transaction rejection must map to specific messages.
D-07: Error recovery must preserve contact information entered in step 1.
D-08: Payment-step errors should be displayed above the payment form so they are immediately visible on mobile.
D-09: MetaMask connection and transaction failures should use explicit inline messaging rather than generic fallback text.
D-10: Wallet errors should provide inline retry affordances for reconnecting the wallet or retrying the transaction.
D-11: Planning must include typecheck/build verification for `FE/artium-web/`.
D-12: Phase deliverables must leave room for verification artifacts required by UX-03.
</decision_map>

<interfaces>
From `FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts`:
```ts
export type PaymentErrorType =
  | 'card_declined'
  | 'insufficient_funds'
  | 'incorrect_cvc'
  | 'expired_card'
  | 'network'
  | 'generic'

export type ClassifiedPaymentError = {
  type: PaymentErrorType
  message: string
}
```

From `FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx`:
```ts
type Props = {
  onWalletConnected: (address: string) => void
  onTxHashReceived: (hash: string) => void
  onWalletDisconnected?: () => void
  onTxHashCleared?: () => void
  errors: FieldErrors<any>
  ethAmount?: number
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define recovery metadata for checkout-level payment failures</name>
  <read_first>
    - FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts
    - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
    - .planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-RESEARCH.md
  </read_first>
  <files>FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts</files>
  <action>Extend the payment error classifier per D-05/D-06 so checkout can tell whether a failure needs a payment-form reset or a retry submission. Add recovery metadata such as a CTA label plus an explicit action enum (`reset-payment` for declines/CVC/expired-card style failures, `retry-submit` for network/setup failures) while keeping the existing human-readable copy. Do not add wallet-currency logic here; wallet connection/transaction messaging stays local to `WalletPaymentSection.tsx` per the Phase 8 scope guardrail.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
  </verify>
  <acceptance_criteria>
    - Decline-style card failures classify to a reset-oriented recovery action.
    - Network/setup failures classify to a resubmit-oriented recovery action.
    - The exported type is rich enough for the checkout banner to render a CTA without hard-coded string branching in the view.
  </acceptance_criteria>
  <done>The checkout view can branch recovery behavior from structured error metadata instead of parsing messages ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Wire retry/reset controls into the checkout payment step</name>
  <read_first>
    - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
    - FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx
    - FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx
    - FE/artium-web/src/@domains/checkout/utils/paymentErrors.ts
    - .planning/phases/6/6-4-SUMMARY.md
  </read_first>
  <files>FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx, FE/artium-web/src/@domains/checkout/components/BuyerCheckoutPaymentForm.tsx, FE/artium-web/src/@domains/checkout/components/WalletPaymentSection.tsx</files>
  <action>Update the payment-step UX per D-06 through D-10. In `BuyerCheckoutPageView.tsx`, render the checkout-level error banner above the payment form with a CTA whose behavior comes from the classifier: reset card/payment-only state for decline-style errors without clearing `contactForm`, and rerun the step-2 payment attempt for network/setup failures using the current form values instead of sending the buyer back to step 1. Keep retry execution semantics explicit: retries always start a fresh frontend payment attempt and must not reuse stale local order/payment state after a failure. In `BuyerCheckoutPaymentForm.tsx`, add the smallest prop/reset wiring needed to remount payment-only controls (Stripe elements and wallet section) while preserving step-1 contact data per D-07. In `WalletPaymentSection.tsx`, keep the existing inline retry buttons but align copy and state clearing with the new checkout banner contract; do not change ETH amount labels or USD→ETH behavior because that belongs to Phase 8.</action>
  <verify>
    <automated>cd FE/artium-web && npx tsc --noemit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Card declines show specific inline copy plus a Try Again control that resets payment-step state only.
    - Network/API failures show a Retry control that reattempts payment from step 2 without wiping step-1 contact fields.
    - Wallet rejection/pending-request flows remain inline with reconnect/retry controls and no new Phase 8 currency behavior.
  </acceptance_criteria>
  <done>Checkout failures are recoverable from the payment step with explicit semantics and preserved buyer-entered contact information.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the Phase 7 validation matrix and evidence checklist</name>
  <read_first>
    - .planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - FE/artium-web/src/@domains/checkout/views/BuyerCheckoutPageView.tsx
  </read_first>
  <files>.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md</files>
  <action>Rewrite the Phase 7 validation map to match the new executable plan/task IDs per D-11/D-12. Replace the old single-plan references with 07-01/07-02 tasks, keep `cd FE/artium-web && npx tsc --noemit` and `cd FE/artium-web && npm run build` as the required automated evidence, and expand the manual checklist so a verifier can capture proof for inline success persistence, Continue Shopping URL cleanup, card decline reset semantics, network retry semantics, and wallet retry controls.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('/Users/dgpthinh/Documents/uit/IE213/project/artium-blockchain/.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md'); s=p.read_text(); assert '07-01' in s and '07-02' in s and 'npx tsc --noemit' in s and 'npm run build' in s" && cd FE/artium-web && npx tsc --noemit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The validation matrix references the new Phase 7 plan/task IDs instead of the legacy single-plan layout.
    - Manual verification steps name the exact retry/success scenarios needed for UX-02 and UX-03 evidence capture.
    - The FE typecheck and build commands appear verbatim in the validation artifact.
  </acceptance_criteria>
  <done>Phase 7 has an executable validation artifact that names both the automated commands and the manual evidence required before completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payment provider / API error → checkout banner | Untrusted error text reaches user-visible recovery UI |
| checkout view → payment-step reset/retry handlers | Client state decides whether to remount payment controls or rerun submission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | I | `paymentErrors.ts` / `BuyerCheckoutPageView.tsx` | mitigate | Map provider/network failures through a fixed classifier and render plain text only; no raw HTML or direct provider message injection into rich UI. |
| T-07-04 | D | `BuyerCheckoutPageView.tsx` retry flow | mitigate | Separate `reset-payment` from `retry-submit` handling so repeated failures do not trap the buyer in stale payment state; retries always rebuild the frontend payment attempt from current form values. |
| T-07-05 | T | `WalletPaymentSection.tsx` | accept | Wallet retry controls remain client-side helpers only and do not bypass backend validation; Phase 8 currency correctness remains out of scope for this phase. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npx tsc --noemit && npm run build`
- Manual scenario 1: force a card decline and confirm the inline banner shows specific decline copy with a Try Again reset action while step-1 contact data remains intact.
- Manual scenario 2: simulate a network or payment-account setup failure and confirm Retry reattempts from step 2 without wiping buyer-entered contact/shipping data.
- Manual scenario 3: reject `eth_requestAccounts` and `eth_sendTransaction` in MetaMask and confirm the wallet section keeps inline retry controls with specific copy.
- Artifact review: open `.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-VALIDATION.md` and confirm the Phase 7 matrix references 07-01 / 07-02 plus the FE commands verbatim.
</verification>

<success_criteria>
- UX-02 is satisfied with separate reset-vs-retry semantics for card/network failures and inline wallet recovery.
- UX-03 is satisfied with a refreshed Phase 7 validation artifact that names the exact automated commands and manual scenarios.
- The plan leaves wallet correctness bugs to Phase 8 instead of widening scope here.
</success_criteria>

<output>
After completion, create `.planning/phases/07-checkout-payment-feedback-success-error-ui-states/07-02-SUMMARY.md`
</output>
