---
phase: 18
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - FE/artium-web/src/@shared/apis/auctionApis.ts
  - FE/artium-web/src/@domains/auction/hooks/useSellerAuctionArtworkCandidates.ts
  - FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
  - FE/artium-web/src/pages/artist/auctions/create.tsx
autonomous: true
requirements: [SAUC-01, SAUC-02, SAUC-03]
must_haves:
  truths:
    - `/artist/auctions/create` exists and uses authenticated seller workspace layout.
    - Frontend calls the seller candidate API without passing `sellerId`.
    - Non-seller users see seller-profile guidance before candidate data is loaded.
    - Eligible and blocked candidates render separately with backend-provided reason copy.
    - No reserve price, duration, bid increment, wallet transaction, or start-auction submission is added in Phase 18.
  artifacts:
    - path: FE/artium-web/src/pages/artist/auctions/create.tsx
      provides: Seller auction artwork picker route.
    - path: FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx
      provides: UI-SPEC-compliant candidate picker view.
    - path: FE/artium-web/src/@shared/apis/auctionApis.ts
      provides: Typed seller candidate API client.
---

<objective>
Build the seller-facing auction artwork picker page that consumes backend eligibility results without duplicating business rules in React.

Purpose: Satisfy SAUC-01, SAUC-02, and SAUC-03 from the user-facing side while preserving Phase 18 scope boundaries.
</objective>

<context>
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-CONTEXT.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-UI-SPEC.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-RESEARCH.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-PATTERNS.md
@FE/artium-web/src/pages/inventory/index.tsx
@FE/artium-web/src/@domains/inventory/components/InventoryArtworkGridViewItem.tsx
@FE/artium-web/src/@domains/quick-sell/views/QuickSellCreateInvoicePageView.tsx
@FE/artium-web/src/@domains/auth/hooks/useRequireAuth.ts
@FE/artium-web/src/@shared/apis/auctionApis.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed frontend seller candidate API</name>
  <read_first>
    - FE/artium-web/src/@shared/apis/auctionApis.ts
    - FE/artium-web/src/@shared/services/apiClient.ts
    - FE/artium-web/src/@shared/apis/artworkApis.ts
  </read_first>
  <files>FE/artium-web/src/@shared/apis/auctionApis.ts</files>
  <action>Add exported TypeScript types `SellerAuctionArtworkEligibilityReason`, `SellerAuctionArtworkRecoveryAction`, `SellerAuctionArtworkCandidate`, and `SellerAuctionArtworkCandidatesResponse` matching the backend DTO names and fields. Add `getSellerArtworkCandidates: () => apiFetch<SellerAuctionArtworkCandidatesResponse>('/auctions/seller/artwork-candidates', { cache: 'no-store' })`. The function must accept no input object and must not include `sellerId` in params, query string, or body.</action>
  <verify>
    <automated>cd FE/artium-web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `auctionApis.ts` contains `getSellerArtworkCandidates`.
    - `getSellerArtworkCandidates` calls `/auctions/seller/artwork-candidates`.
    - `getSellerArtworkCandidates` has no `sellerId` parameter.
    - `auctionApis.ts` exports `SellerAuctionArtworkCandidatesResponse`.
  </acceptance_criteria>
  <done>Frontend has a typed API client for backend-owned eligibility results.</done>
</task>

<task type="auto">
  <name>Task 2: Add candidate loading hook</name>
  <read_first>
    - FE/artium-web/src/@domains/auction/hooks/useAuctionLots.ts
    - FE/artium-web/src/@shared/apis/auctionApis.ts
  </read_first>
  <files>FE/artium-web/src/@domains/auction/hooks/useSellerAuctionArtworkCandidates.ts</files>
  <action>Create `useSellerAuctionArtworkCandidates` that loads `auctionApis.getSellerArtworkCandidates()` on mount, exposes `data`, `eligible`, `blocked`, `isLoading`, `error`, and `refresh`, and initializes `eligible` and `blocked` as empty arrays. Use the same `toError` pattern as `useAuctionLots`. Do not accept `sellerId`. Do not compute eligibility reason codes in the hook.</action>
  <verify>
    <automated>cd FE/artium-web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Hook file contains `export const useSellerAuctionArtworkCandidates`.
    - Hook file calls `auctionApis.getSellerArtworkCandidates()`.
    - Hook function signature has no `sellerId`.
    - Hook file does not contain string literals `NOT_ACTIVE`, `SOLD`, or `ACTIVE_ORDER_LOCK` except through imported types.
  </acceptance_criteria>
  <done>Frontend data loading is scoped to the authenticated backend session.</done>
</task>

<task type="auto">
  <name>Task 3: Build seller auction picker page and route</name>
  <read_first>
    - .planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-UI-SPEC.md
    - FE/artium-web/src/pages/inventory/index.tsx
    - FE/artium-web/src/@shared/components/layout/SidebarLayout.tsx
    - FE/artium-web/src/@shared/components/ui/button.tsx
    - FE/artium-web/src/@domains/auth/hooks/useRequireAuth.ts
    - FE/artium-web/src/@domains/auth/stores/useAuthStore.ts
    - FE/artium-web/src/@shared/types/auth.ts
    - FE/artium-web/src/@domains/inventory/components/InventoryArtworkGridViewItem.tsx
  </read_first>
  <files>FE/artium-web/src/@domains/auction/views/SellerAuctionArtworkPickerPage.tsx, FE/artium-web/src/pages/artist/auctions/create.tsx</files>
  <action>Create route `FE/artium-web/src/pages/artist/auctions/create.tsx` with dynamic import of `SellerAuctionArtworkPickerPage`, `useRequireAuth`, `Metadata title="Choose Artwork for Auction | Artium"`, and `SidebarLayout`. In `SellerAuctionArtworkPickerPage`, read `user` from `useAuthStore`; define seller capability as `user?.roles?.includes('seller')`. If non-seller, render title `Seller profile required`, body `Create or complete your seller profile before starting an auction.`, and CTA `Go to seller profile`. For sellers, use `useSellerAuctionArtworkCandidates`. Render UI-SPEC header copy `SELLER AUCTIONS`, `Choose artwork for auction`, and `We check ownership and auction readiness before you set reserve price, duration, or bid rules.` Render policy cards `Seller-only access`, `Owned artwork only`, and `Blocked reasons shown`. Render eligible section `Ready for auction` with enabled `Select artwork` buttons. Render blocked section `Needs attention` with disabled cards and backend `recoveryActions` messages. Render loading skeletons, backend error copy `We could not load auction eligibility. Try again or return to inventory.`, empty eligible copy `No auction-ready artworks`, and no-artworks copy `No artworks in your inventory yet`. The `Continue to auction terms` affordance may be disabled or link-stubbed, but must not collect terms or submit transactions.</action>
  <verify>
    <automated>cd FE/artium-web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `create.tsx` exists under `src/pages/artist/auctions`.
    - `create.tsx` uses `SidebarLayout`.
    - `SellerAuctionArtworkPickerPage.tsx` contains `Choose artwork for auction`.
    - `SellerAuctionArtworkPickerPage.tsx` contains `Seller profile required`.
    - `SellerAuctionArtworkPickerPage.tsx` contains `Ready for auction` and `Needs attention`.
    - `SellerAuctionArtworkPickerPage.tsx` does not contain `reservePrice`, `minBidIncrement`, `auctionDuration`, `createAuction`, or `submitAuction`.
  </acceptance_criteria>
  <done>Seller can access the auction artwork picker and see backend eligibility explanations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser auth store to UI gate | Client-side roles affect display guidance only. |
| backend eligibility to React rendering | Backend reason codes and recovery copy drive the picker UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Spoofing | frontend seller gate | accept with mitigation | Treat frontend role checks as convenience only; Plan 02 backend endpoint remains authoritative. |
| T-18-08 | Tampering | candidate API request | mitigate | API function accepts no `sellerId`; backend derives identity. |
| T-18-09 | Information Disclosure | blocked reason rendering | mitigate | Render only reason codes/messages returned by backend, not raw order details. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npm run lint`
- Manual: unauthenticated `/artist/auctions/create` does not render picker data.
- Manual: non-seller sees `Seller profile required`.
- Manual: seller sees `Ready for auction` and `Needs attention` sections from backend response.
- Manual: browser network request to `/auctions/seller/artwork-candidates` contains no `sellerId` query parameter.
</verification>

<success_criteria>
- Seller auction picker route exists in the seller workspace.
- UI follows `18-UI-SPEC.md` copy, layout, and state contracts.
- Frontend consumes backend eligibility results without duplicating reason logic.
- Phase 19/20 functionality is not implemented in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-03-SUMMARY.md`.
</output>
