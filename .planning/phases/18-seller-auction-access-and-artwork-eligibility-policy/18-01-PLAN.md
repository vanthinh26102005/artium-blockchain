---
phase: 18
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BE/libs/common/src/dtos/auctions/seller-auction-artwork-candidates.dto.ts
  - BE/libs/common/src/dtos/auctions/index.ts
  - BE/apps/artwork-service/src/application/queries/artworks/ListSellerAuctionArtworkCandidates.query.ts
  - BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.ts
  - BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.spec.ts
  - BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts
  - BE/apps/artwork-service/src/app.module.ts
autonomous: true
requirements: [SAUC-02, SAUC-03]
must_haves:
  truths:
    - Artwork eligibility is computed server-side from seller-owned artwork records.
    - The artwork-service candidate query only accepts a sellerId from trusted backend callers.
    - Intrinsic blockers include lifecycle status, publication state, on-chain auction ID, quantity, primary image, and display metadata.
    - Every blocked artwork has stable reason codes and recovery actions.
  artifacts:
    - path: BE/libs/common/src/dtos/auctions/seller-auction-artwork-candidates.dto.ts
      provides: Shared eligibility reason enum and candidate response DTOs.
    - path: BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.ts
      provides: Intrinsic artwork eligibility policy.
---

<objective>
Create the shared DTO contract and artwork-service eligibility query for seller auction artwork candidates.

Purpose: Satisfy SAUC-02 and SAUC-03 for intrinsic artwork state before active order locks are merged by the gateway in Plan 02.
</objective>

<context>
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-CONTEXT.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-UI-SPEC.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-RESEARCH.md
@.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-PATTERNS.md
@BE/apps/artwork-service/src/domain/entities/artworks.entity.ts
@BE/libs/common/src/enums/artwork-status.enum.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define seller auction candidate DTOs</name>
  <read_first>
    - BE/libs/common/src/dtos/auctions/auction-read.dto.ts
    - BE/libs/common/src/dtos/auctions/get-auctions.dto.ts
    - BE/libs/common/src/dtos/auctions/index.ts
    - BE/libs/common/src/index.ts
  </read_first>
  <files>BE/libs/common/src/dtos/auctions/seller-auction-artwork-candidates.dto.ts, BE/libs/common/src/dtos/auctions/index.ts</files>
  <action>Create `SellerAuctionArtworkEligibilityReason` enum with exact values `NOT_ACTIVE`, `NOT_PUBLISHED`, `SOLD`, `DELETED`, `RESERVED`, `IN_AUCTION`, `HAS_ON_CHAIN_AUCTION`, `ACTIVE_ORDER_LOCK`, `MULTI_QUANTITY`, `MISSING_PRIMARY_IMAGE`, and `MISSING_METADATA`. Create `SellerAuctionArtworkRecoveryActionObject` with `reasonCode`, `message`, and `actionLabel`. Create `SellerAuctionArtworkCandidateObject` with `artworkId`, `sellerId`, `title`, `creatorName`, `thumbnailUrl`, `status`, `isPublished`, `quantity`, `onChainAuctionId`, `isEligible`, `reasonCodes`, and `recoveryActions`. Create `SellerAuctionArtworkCandidatesResponse` with `eligible`, `blocked`, `total`, `eligibleCount`, and `blockedCount`. Export the new file from `BE/libs/common/src/dtos/auctions/index.ts`.</action>
  <verify>
    <automated>cd BE && yarn build:artwork</automated>
  </verify>
  <acceptance_criteria>
    - `seller-auction-artwork-candidates.dto.ts` contains `export enum SellerAuctionArtworkEligibilityReason`.
    - `seller-auction-artwork-candidates.dto.ts` contains `ACTIVE_ORDER_LOCK`.
    - `seller-auction-artwork-candidates.dto.ts` contains `export class SellerAuctionArtworkCandidatesResponse`.
    - `BE/libs/common/src/dtos/auctions/index.ts` exports `./seller-auction-artwork-candidates.dto`.
  </acceptance_criteria>
  <done>Shared backend/frontend DTO names are available from `@app/common`.</done>
</task>

<task type="auto">
  <name>Task 2: Add artwork-service intrinsic eligibility query</name>
  <read_first>
    - BE/apps/artwork-service/src/application/queries/artworks/ListArtworks.query.ts
    - BE/apps/artwork-service/src/application/queries/artworks/handlers/ListArtworks.query.handler.ts
    - BE/apps/artwork-service/src/domain/interfaces/artwork.repository.interface.ts
    - BE/apps/artwork-service/src/infrastructure/repositories/artwork.repository.ts
    - BE/apps/artwork-service/src/domain/entities/artworks.entity.ts
    - BE/apps/artwork-service/src/app.module.ts
  </read_first>
  <files>BE/apps/artwork-service/src/application/queries/artworks/ListSellerAuctionArtworkCandidates.query.ts, BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.ts, BE/apps/artwork-service/src/presentation/microservice/artworks.microservice.controller.ts, BE/apps/artwork-service/src/app.module.ts</files>
  <action>Create `ListSellerAuctionArtworkCandidatesQuery` accepting `sellerId: string`. Create a handler that loads `repo.findManyBySellerId(sellerId, { orderBy: { createdAt: 'DESC' } })`, maps every owned artwork into `SellerAuctionArtworkCandidateObject`, and computes intrinsic reason codes. Add `SOLD` when `status === ArtworkStatus.SOLD`; `DELETED` for `DELETED`; `RESERVED` for `RESERVED`; `IN_AUCTION` for `IN_AUCTION`; `NOT_ACTIVE` for any status other than `ACTIVE` unless a more specific lifecycle reason already applies; `NOT_PUBLISHED` when `isPublished !== true`; `HAS_ON_CHAIN_AUCTION` when `onChainAuctionId` is non-empty; `MULTI_QUANTITY` when `quantity !== 1`; `MISSING_PRIMARY_IMAGE` when `images` has no item with `isPrimary === true`; and `MISSING_METADATA` when `title` or `creatorName` is missing/blank. Build `recoveryActions` from the exact UI-SPEC copy where applicable. Set `isEligible` only when `reasonCodes.length === 0`. Return grouped `eligible`, `blocked`, `total`, `eligibleCount`, and `blockedCount`. Register the handler in `ArtworkServiceModule.QueryHandlers`. Add microservice pattern `{ cmd: 'list_seller_auction_artwork_candidates' }` to `ArtworkMicroserviceController`.</action>
  <verify>
    <automated>cd BE && yarn build:artwork</automated>
  </verify>
  <acceptance_criteria>
    - `ListSellerAuctionArtworkCandidates.query.ts` contains `constructor(public readonly sellerId: string)`.
    - Handler calls `findManyBySellerId`.
    - Handler contains `SellerAuctionArtworkEligibilityReason.MULTI_QUANTITY`.
    - Handler checks for `image.isPrimary === true`.
    - `ArtworkMicroserviceController` contains `list_seller_auction_artwork_candidates`.
    - `BE/apps/artwork-service/src/app.module.ts` registers `ListSellerAuctionArtworkCandidatesHandler`.
  </acceptance_criteria>
  <done>Artwork-service can return owned candidate artworks with intrinsic eligibility reasons.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for intrinsic reason codes</name>
  <read_first>
    - BE/package.json
    - BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.ts
  </read_first>
  <files>BE/apps/artwork-service/src/application/queries/artworks/handlers/ListSellerAuctionArtworkCandidates.query.handler.spec.ts</files>
  <action>Create Jest table tests for `ListSellerAuctionArtworkCandidatesHandler`. Mock `IArtworkRepository.findManyBySellerId`. Include cases for eligible artwork, `SOLD`, `DELETED`, `RESERVED`, `IN_AUCTION`, draft/inactive as `NOT_ACTIVE`, unpublished as `NOT_PUBLISHED`, existing `onChainAuctionId` as `HAS_ON_CHAIN_AUCTION`, `quantity: 2` as `MULTI_QUANTITY`, missing primary image as `MISSING_PRIMARY_IMAGE`, and blank `creatorName` as `MISSING_METADATA`. Assert grouped response counts and that `recoveryActions` contains the same reason code as each blocker.</action>
  <verify>
    <automated>cd BE && yarn test --runInBand ListSellerAuctionArtworkCandidates</automated>
  </verify>
  <acceptance_criteria>
    - Spec file contains `describe('ListSellerAuctionArtworkCandidatesHandler'`.
    - Spec file contains every reason code from `SellerAuctionArtworkEligibilityReason` except `ACTIVE_ORDER_LOCK`.
    - `cd BE && yarn test --runInBand ListSellerAuctionArtworkCandidates` exits 0.
  </acceptance_criteria>
  <done>Intrinsic eligibility policy has automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sellerId to artwork-service | Gateway will pass authenticated seller identity into a backend-only microservice message. |
| artwork record to UI DTO | Internal artwork entity fields become seller-visible eligibility metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | `ListSellerAuctionArtworkCandidatesQuery` | mitigate | Do not expose this microservice query directly to clients; Plan 02 gateway derives seller identity from JWT. |
| T-18-05 | Information Disclosure | Candidate DTO mapping | mitigate | Return only seller-owned artwork display and eligibility fields; do not add private internal metadata. |
</threat_model>

<verification>
- Automated: `cd BE && yarn test --runInBand ListSellerAuctionArtworkCandidates`
- Automated: `cd BE && yarn build:artwork`
</verification>

<success_criteria>
- Shared DTOs exist for seller auction candidates.
- Artwork-service returns eligible and blocked owned artworks.
- Intrinsic blocked reason codes are stable and covered by tests.
</success_criteria>

<output>
After completion, create `.planning/phases/18-seller-auction-access-and-artwork-eligibility-policy/18-01-SUMMARY.md`.
</output>
