---
phase: 34
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - FE/artium-web/src/@domains/profile/views/ProfilePage.tsx
  - FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx
autonomous: true
requirements: [PMED-05, PMED-06]
requirements_addressed: [PMED-05, PMED-06]
must_haves:
  truths:
    - D-01 through D-04: Profile page integration must respect upload-first state and disabled publish behavior supplied by the composer.
    - D-05 through D-08 and PMED-10: the visible modal must be the Orders-style two-zone device upload composer with no pasted-link creation path.
    - D-09 through D-12 and PMED-05: creation submits exactly one uploaded `mediaId` plus optional caption, location, hashtags, pinning, and duration when supported.
    - D-13 through D-16 and PMED-06: retry, replace, cancel, and close behavior preserve or reset state according to the Phase 34 context decisions.
    - Existing moment list/detail behavior remains in place by continuing to prepend `mapMomentToProfileMoment(created)` into `createdMoments`.
  artifacts:
    - path: FE/artium-web/src/@domains/profile/views/ProfilePage.tsx
      provides: Owner profile integration for the device upload composer and `profileApis.createMoment(input)` submission.
---

<objective>
Replace the old URL-based moment modal in `ProfilePage.tsx` with `MomentDeviceUploadComposer`, submit moments through `CreateMomentInput.mediaId`, preserve existing moment list update behavior, and run final structural/type/lint verification proving the pasted-link path is gone.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-moment-device-upload-composer/34-CONTEXT.md
@.planning/phases/34-moment-device-upload-composer/34-UI-SPEC.md
@.planning/phases/34-moment-device-upload-composer/34-RESEARCH.md
@.planning/phases/34-moment-device-upload-composer/34-VALIDATION.md
@.planning/phases/34-moment-device-upload-composer/34-PATTERNS.md
@FE/artium-web/src/@domains/profile/views/ProfilePage.tsx
@FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx
@FE/artium-web/src/@shared/apis/profileApis.ts
@FE/artium-web/src/@domains/profile/utils/profileApiMapper.ts
@FE/artium-web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ProfilePage URL moment modal with device upload composer integration</name>
  <files>FE/artium-web/src/@domains/profile/views/ProfilePage.tsx</files>
  <read_first>
    - FE/artium-web/src/@domains/profile/views/ProfilePage.tsx
    - FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx
    - FE/artium-web/src/@shared/apis/profileApis.ts
    - FE/artium-web/src/@domains/profile/utils/profileApiMapper.ts
    - .planning/phases/34-moment-device-upload-composer/34-CONTEXT.md
    - .planning/phases/34-moment-device-upload-composer/34-UI-SPEC.md
  </read_first>
  <action>Update `ProfilePage.tsx` to import `MomentDeviceUploadComposer` from `@domains/profile/components/MomentDeviceUploadComposer` and import `type CreateMomentInput` from `@shared/apis/profileApis`. Remove the `momentForm` state object, `resetMomentForm`, and old `handleCreateMoment(event: FormEvent<HTMLFormElement>)` implementation that validates `mediaUrl`. Replace it with `const handleCreateMoment = async (input: CreateMomentInput) => { if (!isAuthenticated || momentSubmitting) return; setMomentSubmitting(true); setMomentError(null); setMomentSuccess(null); try { const created = await profileApis.createMoment(input); setCreatedMoments((prev) => [mapMomentToProfileMoment(created), ...prev]); setMomentSuccess('Moment published.'); setIsMomentDialogOpen(false); } catch (err) { setMomentError(err instanceof Error ? err.message : 'Failed to create moment.'); } finally { setMomentSubmitting(false); } }`. Keep `parseTags` only if the moodboard flow still needs it; the new moment composer owns hashtag parsing. Replace the entire old moment `Dialog` JSX with `<MomentDeviceUploadComposer open={isMomentDialogOpen} submitting={momentSubmitting} errorMessage={momentError} onOpenChange={(open) => { setIsMomentDialogOpen(open); if (!open) { setMomentSubmitting(false); setMomentError(null); } }} onPublish={handleCreateMoment} />`. Update the `New moment` button click handler to set `setMomentError(null)`, `setMomentSuccess(null)`, and `setIsMomentDialogOpen(true)` without calling `resetMomentForm`. Do not change moodboard modal behavior in this phase.</action>
  <verify>
    <automated>rg -n "MomentDeviceUploadComposer|CreateMomentInput|profileApis\\.createMoment\\(input\\)|setCreatedMoments|Moment published" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx</automated>
    <automated>! rg -n "momentForm|resetMomentForm|mediaUrl|thumbnailUrl|Paste a media URL|Media URL|Thumbnail URL|Tell your story in motion" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx</automated>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
  </verify>
  <acceptance_criteria>
    - `ProfilePage.tsx` contains `MomentDeviceUploadComposer`.
    - `ProfilePage.tsx` contains `CreateMomentInput`.
    - `ProfilePage.tsx` contains `profileApis.createMoment(input)`.
    - `ProfilePage.tsx` contains `setCreatedMoments((prev) => [mapMomentToProfileMoment(created), ...prev])`.
    - `ProfilePage.tsx` does not contain `momentForm`.
    - `ProfilePage.tsx` does not contain `mediaUrl`.
    - `ProfilePage.tsx` does not contain `thumbnailUrl`.
    - `ProfilePage.tsx` does not contain `Media URL`.
    - `cd FE/artium-web && npx tsc --noemit` exits 0 or unrelated pre-existing failures are documented in `34-02-SUMMARY.md`.
  </acceptance_criteria>
  <done>Profile owner moment creation now opens the device upload composer and creates moments from `mediaId` proof.</done>
</task>

<task type="auto">
  <name>Task 2: Run final structural, type, lint, and build verification for Phase 34</name>
  <files>FE/artium-web/src/@domains/profile/views/ProfilePage.tsx, FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx, FE/artium-web/src/@domains/profile/hooks/useProfileMomentUpload.ts</files>
  <read_first>
    - FE/artium-web/package.json
    - FE/artium-web/src/@domains/profile/views/ProfilePage.tsx
    - FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx
    - FE/artium-web/src/@domains/profile/hooks/useProfileMomentUpload.ts
    - .planning/phases/34-moment-device-upload-composer/34-VALIDATION.md
  </read_first>
  <action>Run final Phase 34 checks. First run `rg -n "uploadProfileMomentMedia|PROFILE_MOMENT_ACCEPT|Drop one image or video|Uploading media|Upload complete|Retry upload|Replace file|Publish moment|profileApis.createMoment\\(input\\)" FE/artium-web/src/@domains/profile`. Then run `! rg -n "Paste a media URL|Media URL|Thumbnail URL|momentForm|mediaUrl|thumbnailUrl" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx`. Then run `cd FE/artium-web && npx tsc --noemit` and `cd FE/artium-web && npm run lint`. If either TypeScript or lint fails because of Phase 34 files, fix the relevant profile composer/hook/page issue and rerun. If failures are unrelated and pre-existing, document exact commands and failure summaries in `34-02-SUMMARY.md`. Run `cd FE/artium-web && npm run build` if required environment variables are present; otherwise record why build was not run.</action>
  <verify>
    <automated>rg -n "uploadProfileMomentMedia|PROFILE_MOMENT_ACCEPT|Drop one image or video|Uploading media|Upload complete|Retry upload|Replace file|Publish moment|profileApis\\.createMoment\\(input\\)" FE/artium-web/src/@domains/profile</automated>
    <automated>! rg -n "Paste a media URL|Media URL|Thumbnail URL|momentForm|mediaUrl|thumbnailUrl" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx</automated>
    <automated>cd FE/artium-web && npx tsc --noemit</automated>
    <automated>cd FE/artium-web && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Structural grep finds `uploadProfileMomentMedia` in the profile domain.
    - Structural grep finds `Drop one image or video` in the profile domain.
    - Structural grep finds `profileApis.createMoment(input)` in `ProfilePage.tsx`.
    - Negative structural grep finds no `Paste a media URL`.
    - Negative structural grep finds no `Media URL`.
    - Negative structural grep finds no `Thumbnail URL`.
    - Negative structural grep finds no `momentForm`.
    - `cd FE/artium-web && npx tsc --noemit` exits 0 or `34-02-SUMMARY.md` documents unrelated pre-existing failures.
    - `cd FE/artium-web && npm run lint` exits 0 or `34-02-SUMMARY.md` documents unrelated pre-existing failures.
  </acceptance_criteria>
  <done>Phase 34 has structural and frontend verification evidence for the mediaId upload composer path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Composer -> profile create API | Profile page must submit only backend-issued upload proof, not client-provided URLs. |
| Profile page -> existing moment rendering | Created moments must continue through existing mapper/list behavior without bypassing owner controls or responsive cards. |
| Verification commands -> phase closure | Type/lint/build evidence must distinguish Phase 34 regressions from unrelated existing failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Spoofing | `ProfilePage.tsx` create call | mitigate | Replace URL payload construction with `profileApis.createMoment(input)` where `input.mediaId` comes from the composer upload success. |
| T-34-06 | Tampering | old URL modal path | mitigate | Remove `momentForm`, media URL inputs, thumbnail URL inputs, and manual media type toggle from `ProfilePage.tsx`. |
| T-34-07 | Denial of Service | integration regressions | mitigate | Run structural checks plus TypeScript and lint after integration. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npx tsc --noemit`
- Automated: `cd FE/artium-web && npm run lint`
- Structural: `rg -n "MomentDeviceUploadComposer|CreateMomentInput|profileApis\\.createMoment\\(input\\)" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx`
- Structural: `! rg -n "momentForm|mediaUrl|thumbnailUrl|Paste a media URL|Media URL|Thumbnail URL" FE/artium-web/src/@domains/profile/views/ProfilePage.tsx FE/artium-web/src/@domains/profile/components/MomentDeviceUploadComposer.tsx`
</verification>

<success_criteria>
- Profile owners open the new device upload composer from the existing `New moment` entry point.
- `ProfilePage.tsx` no longer contains the pasted media URL moment creation path.
- Moment creation submits `CreateMomentInput` with required `mediaId`.
- Successful creation still prepends `mapMomentToProfileMoment(created)` to `createdMoments`.
- TypeScript and lint verification have run, with any unrelated blockers documented.
</success_criteria>

<output>
After completion, create `.planning/phases/34-moment-device-upload-composer/34-02-SUMMARY.md`.
</output>
