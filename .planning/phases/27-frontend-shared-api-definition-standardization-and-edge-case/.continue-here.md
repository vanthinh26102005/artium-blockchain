---
context: phase
phase: 27-frontend-shared-api-definition-standardization-and-edge-case
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-29T05:00:14.469Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: State-source reconciliation - `STATE.md` had a stale Phase 20 `stopped_at` marker mixed with Phase 27 execution state. Before acting, reconcile `STATE.md`, `roadmap.analyze`, and artifact counts; do not route from `stopped_at` alone.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Stale state marker routing | `STATE.md` showed `stopped_at: Phase 20 context gathered` while its current position and roadmap artifacts showed Phase 27 execution. This led to uncertainty about what to commit/resume. | blocking | Run `gsd-sdk query roadmap.analyze`, inspect the active phase directory, and compare plan/summary counts before choosing the resume target. |
| Broad staging in dirty worktree | The worktree contains many unrelated source and planning changes. Accidentally staging all changes would mix handoff, planning, and unrelated implementation work. | advisory | Stage explicit files only. Never use `git add .` or broad staging for this repository state. |

<current_state>
Phase 27 is the active execution context: `Frontend shared API definition standardization and edge-case audit`.

Plan 27-01 is in progress. Its objective is to create the shared frontend API client contract used by every `FE/artium-web/src/@shared/apis` module.

Evidence:
- `roadmap.analyze` reports current phase `27`.
- `.planning/STATE.md` Current Position says Phase 27, Plan 1 of 4, executing.
- `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/` contains `27-01-PLAN.md` through `27-04-PLAN.md`, with no `27-01-SUMMARY.md`.
- `FE/artium-web/src/@shared/services/apiClient.ts` already contains the requested helper/error markers.
- `FE/artium-web/src/@shared/apis/README.md` already exists and contains the required standard sections.

Do not resume Phase 20 execution from this handoff. Phase 20 context was captured and committed separately as `e808e515 docs(20): capture phase context`.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Extend shared API client primitives - appears implemented but not verified.
- Task 2: Preserve structured error details and empty success responses - appears implemented but not verified.
- Task 3: Document the API module standard - appears implemented but not verified.

Observed implementation markers:
- `apiClient.ts` exports `ApiQueryPrimitive`, `ApiQueryValue`, `buildApiUrl`, `buildQueryString`, `withQuery`, `encodePathSegment`, `jsonBody`, and `ApiError`.
- `apiClient.ts` includes `response.status === 204`, `response.text()`, `Array.isArray(message)`, `error.data`, and `error.headers`.
- `FE/artium-web/src/@shared/apis/README.md` includes the plan-required sections and helper names.
</completed_work>

<remaining_work>

- Run the structural `rg` checks from `27-01-PLAN.md`.
- Run `cd FE/artium-web && npm run lint`.
- Run `cd FE/artium-web && npm run build`.
- Review the diff for `FE/artium-web/src/@shared/services/apiClient.ts` and `FE/artium-web/src/@shared/apis/README.md`.
- If verification passes, create `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-01-SUMMARY.md`.
- Commit only plan 27-01 files, not unrelated worktree changes.
</remaining_work>

<decisions_made>

- Use Phase 27 as the active resume target because `roadmap.analyze`, the phase directory, and `STATE.md` Current Position all point there.
- Treat `init.progress.current_phase` returning phase 7 as stale or unreliable for this pause.
- Keep Phase 20 context separate; it was already committed in `e808e515`.
</decisions_made>

<blockers>
- `STATE.md` still needs a clean session marker that matches Phase 27; use `gsd-sdk query state.record-session` rather than editing the file by hand.
- Large unrelated source changes exist outside the active Phase 27 plan. They may be user work. Do not revert them and do not include them in Phase 27 plan 01 commits unless they are explicitly part of the plan.
</blockers>

## Required Reading (in order)
1. `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-01-PLAN.md` - exact current plan tasks, acceptance criteria, and verification commands.
1. `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-CONTEXT.md` - locked phase decisions and scope boundaries.
1. `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-RESEARCH.md` - implementation guidance and API audit findings.
1. `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-PATTERNS.md` - local codebase patterns to preserve.
1. `FE/artium-web/src/@shared/services/apiClient.ts` - changed shared client implementation.
1. `FE/artium-web/src/@shared/apis/README.md` - new API module standard.
1. `.planning/METHODOLOGY.md` (if it exists) - project analytical lenses; apply before any assumption analysis.

## Critical Anti-Patterns (do NOT repeat these)
- STATE-only routing: do not trust `stopped_at` alone. Reconcile state with roadmap/artifacts first.
- Broad staging in a dirty worktree: do not stage unrelated changes or use `git add .`.

## Infrastructure State
- No dev server or watcher was started by the pause workflow.
- No tests have been run for plan 27-01 in this pause session.
- The worktree is dirty before resume. Important unrelated changes include backend env/deployment files, frontend profile/API files, deleted legacy `smart-contracts/*`, and new `contracts/`.

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-01-PLAN.md`
- Critique focus: confirm `apiClient.ts` helper signatures match current callers, `apiUpload` does not break existing upload API usage, and README rules match actual available helpers.
- Gate: Do not create `27-01-SUMMARY.md` until structural checks, lint, and build are run or explicitly documented as blocked.

<context>
The immediate goal is not to redesign Phase 27. It is to resume plan 27-01, verify the already-present shared API client and README changes, then produce the plan summary if verification passes.

The recent Phase 20 discussion created `20-CONTEXT.md` and `20-DISCUSSION-LOG.md`, but that was planning context capture only. It should not alter Phase 27 execution.
</context>

<next_action>
Start with: `rg -n "export type ApiQueryPrimitive|export type ApiQueryValue|export const buildApiUrl|export const buildQueryString|export const withQuery|export const encodePathSegment|export const jsonBody|export type ApiError|response.status === 204|response.text\\(\\)|Array.isArray\\(message\\)|error.data|error.headers" FE/artium-web/src/@shared/services/apiClient.ts`
</next_action>
