---
phase: 27
plan: 04
type: execute
wave: 4
depends_on: ["27-01", "27-02", "27-03"]
files_modified:
  - FE/artium-web/src/@shared/apis/README.md
  - FE/artium-web/src/@shared/services/apiClient.ts
  - FE/artium-web/src/@shared/apis/artworkApis.ts
  - FE/artium-web/src/@shared/apis/artworkFolderApis.ts
  - FE/artium-web/src/@shared/apis/auctionApis.ts
  - FE/artium-web/src/@shared/apis/orderApis.ts
  - FE/artium-web/src/@shared/apis/paymentApis.ts
  - FE/artium-web/src/@shared/apis/profileApis.ts
  - FE/artium-web/src/@shared/apis/followersApis.ts
  - FE/artium-web/src/@shared/apis/eventsApis.ts
  - FE/artium-web/src/@shared/apis/usersApi.ts
  - FE/artium-web/src/@shared/apis/invoiceApis.ts
  - FE/artium-web/src/@shared/apis/invoiceMocks.ts
  - FE/artium-web/src/@shared/apis/messagingApis.ts
  - FE/artium-web/src/@shared/apis/artworkUploadApi.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - D-10 is implemented by verifying with the existing frontend lint and build commands.
    - All Phase 27 edge cases are represented in the README and structural checks.
    - No broad consumer import migration is required because default API object exports stay stable.
  artifacts:
    - path: FE/artium-web/src/@shared/apis/README.md
      provides: Final API definition standard and edge-case checklist.
---

<objective>
Run the final API edge-case audit, documentation pass, and frontend verification.

Purpose: ensure the standardized API layer compiles, follows the documented rules, avoids leftover duplicated/manual URL logic, and leaves a clear checklist for future API definitions.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-CONTEXT.md
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-RESEARCH.md
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-PATTERNS.md
@FE/artium-web/src/@shared/apis/README.md
@FE/artium-web/src/@shared/services/apiClient.ts
@FE/artium-web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all shared API modules for leftover manual URL/query/path behavior</name>
  <read_first>
    - FE/artium-web/src/@shared/apis/README.md
    - FE/artium-web/src/@shared/services/apiClient.ts
    - FE/artium-web/src/@shared/apis/artworkApis.ts
    - FE/artium-web/src/@shared/apis/artworkFolderApis.ts
    - FE/artium-web/src/@shared/apis/auctionApis.ts
    - FE/artium-web/src/@shared/apis/orderApis.ts
    - FE/artium-web/src/@shared/apis/paymentApis.ts
    - FE/artium-web/src/@shared/apis/profileApis.ts
    - FE/artium-web/src/@shared/apis/followersApis.ts
    - FE/artium-web/src/@shared/apis/eventsApis.ts
    - FE/artium-web/src/@shared/apis/usersApi.ts
    - FE/artium-web/src/@shared/apis/invoiceApis.ts
    - FE/artium-web/src/@shared/apis/messagingApis.ts
    - FE/artium-web/src/@shared/apis/artworkUploadApi.ts
  </read_first>
  <files>FE/artium-web/src/@shared/apis/*.ts, FE/artium-web/src/@shared/services/apiClient.ts</files>
  <action>Inspect every file in `FE/artium-web/src/@shared/apis` for leftover duplicated query helpers, raw query interpolation, raw dynamic route interpolation, direct `fetch`, and per-module auth token handling. Fix remaining violations by using `withQuery`, `encodePathSegment`, `apiFetch`, `apiPost`, and `apiUpload`. It is acceptable for `invoiceMocks.ts` to contain mock-only helper logic and for API modules to contain response normalizers such as `normalizeArtworkList`.</action>
  <verify>
    <automated>! rg -n "const buildQuery|const buildQueryString|new URLSearchParams|fetch\\(|useAuthStore" FE/artium-web/src/@shared/apis</automated>
    <automated>rg -n "withQuery|encodePathSegment|apiUpload|apiFetch|apiPost" FE/artium-web/src/@shared/apis FE/artium-web/src/@shared/services/apiClient.ts</automated>
  </verify>
  <acceptance_criteria>
    - No file under `FE/artium-web/src/@shared/apis` contains `const buildQuery`.
    - No file under `FE/artium-web/src/@shared/apis` contains `const buildQueryString`.
    - No file under `FE/artium-web/src/@shared/apis` contains `new URLSearchParams`.
    - No file under `FE/artium-web/src/@shared/apis` contains `fetch(`.
    - No file under `FE/artium-web/src/@shared/apis` imports `useAuthStore`.
    - At least one file under `FE/artium-web/src/@shared/apis` contains `withQuery`.
    - At least one file under `FE/artium-web/src/@shared/apis` contains `encodePathSegment`.
    - At least one file under `FE/artium-web/src/@shared/apis` contains `apiUpload`.
  </acceptance_criteria>
  <done>Shared API modules no longer contain duplicated/manual request plumbing outside the documented helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize edge-case documentation with migration examples</name>
  <read_first>
    - FE/artium-web/src/@shared/apis/README.md
    - .planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-RESEARCH.md
  </read_first>
  <files>FE/artium-web/src/@shared/apis/README.md</files>
  <action>Update `README.md` to include a `## Migration Examples` section with three concrete examples: query construction using `withQuery('/orders', { scope, status, skip, take })`, path construction using `` `/orders/${encodePathSegment(id)}` ``, and upload construction using `apiUpload('/messaging/upload', formData, { baseUrl })`. Add a `## Edge Case Checklist` section listing `204 No Content`, string-array backend messages, query value `0`, query value `false`, array query values, IDs containing `/`, `?`, `#`, or spaces, `NEXT_PUBLIC_ARTWORK_API_URL` ending with `/artwork`, `NEXT_PUBLIC_ARTWORK_API_URL` ending with `/artworks`, and `FormData` content-type boundaries.</action>
  <verify>
    <automated>rg -n "## Migration Examples|withQuery\\('/orders'|encodePathSegment\\(id\\)|apiUpload\\('/messaging/upload'|## Edge Case Checklist|204 No Content|string-array|query value `0`|query value `false`|array query values|IDs containing|/artwork|/artworks|FormData" FE/artium-web/src/@shared/apis/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `## Migration Examples`.
    - `README.md` contains `withQuery('/orders'`.
    - `README.md` contains `encodePathSegment(id)`.
    - `README.md` contains `apiUpload('/messaging/upload'`.
    - `README.md` contains `## Edge Case Checklist`.
    - `README.md` contains `204 No Content`.
    - `README.md` contains `query value \`0\``.
    - `README.md` contains `query value \`false\``.
    - `README.md` contains `/artwork`.
    - `README.md` contains `/artworks`.
    - `README.md` contains `FormData`.
  </acceptance_criteria>
  <done>The documentation captures concrete migration examples and the edge cases that motivated this phase.</done>
</task>

<task type="auto">
  <name>Task 3: Run frontend verification and fix API-layer type/lint/build issues</name>
  <read_first>
    - FE/artium-web/package.json
    - FE/artium-web/tsconfig.json
    - FE/artium-web/src/@shared/services/apiClient.ts
    - FE/artium-web/src/@shared/apis/README.md
  </read_first>
  <files>FE/artium-web/src/@shared/services/apiClient.ts, FE/artium-web/src/@shared/apis/*.ts</files>
  <action>Run `cd FE/artium-web && npm run lint` and `cd FE/artium-web && npm run build`. If either command fails because of Phase 27 API-layer changes, fix the relevant API module or shared client issue and rerun the command. Do not fix unrelated lint/build failures outside the API layer unless they are directly caused by this phase. Record unrelated pre-existing failures in the plan summary if they block full verification.</action>
  <verify>
    <automated>cd FE/artium-web && npm run lint</automated>
    <automated>cd FE/artium-web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd FE/artium-web && npm run lint` exits 0, or the summary names each unrelated pre-existing failure that blocks it.
    - `cd FE/artium-web && npm run build` exits 0, or the summary names each unrelated pre-existing failure that blocks it.
    - No TypeScript error references `FE/artium-web/src/@shared/services/apiClient.ts`.
    - No TypeScript error references a file under `FE/artium-web/src/@shared/apis` unless the summary explains it as unrelated pre-existing behavior.
  </acceptance_criteria>
  <done>Frontend lint/build verification has run and API-layer regressions are fixed or clearly isolated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API layer -> all frontend domains | A request helper regression can break checkout, auctions, inventory, messaging, events, and profile flows. |
| Documentation -> future implementation | Incomplete guidance can reintroduce duplicated/manual request behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-10 | Denial of Service | API-layer regression | mitigate | Run frontend lint and build after all migrations and fix API-layer failures before completion. |
| T-27-11 | Tampering | future API definitions | mitigate | Document migration examples and edge-case checklist to prevent raw path/query construction from returning. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npm run lint`
- Automated: `cd FE/artium-web && npm run build`
- Structural: `! rg -n "const buildQuery|const buildQueryString|new URLSearchParams|fetch\\(|useAuthStore" FE/artium-web/src/@shared/apis`
- Structural: `rg -n "## Edge Case Checklist|204 No Content|query value \`0\`|query value \`false\`|FormData" FE/artium-web/src/@shared/apis/README.md`
</verification>

<success_criteria>
- Shared API modules no longer duplicate query builders or bypass shared auth/error/upload helpers.
- The API standard README includes concrete migration examples and edge-case checklist.
- Frontend lint and build have been run, with API-layer regressions fixed or explicitly isolated.
</success_criteria>

<output>
After completion, create `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-04-SUMMARY.md`.
</output>
