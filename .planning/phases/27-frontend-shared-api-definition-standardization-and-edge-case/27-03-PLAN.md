---
phase: 27
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - FE/artium-web/src/@shared/services/apiClient.ts
  - FE/artium-web/src/@shared/apis/artworkUploadApi.ts
  - FE/artium-web/src/@shared/apis/messagingApis.ts
  - FE/artium-web/src/@shared/apis/invoiceApis.ts
  - FE/artium-web/src/@shared/apis/invoiceMocks.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - D-08 is implemented by moving reusable upload/progress behavior into a shared upload helper while preserving XHR progress support.
    - D-09 is implemented by isolating invoice mock behavior from production invoice endpoint definitions.
    - D-01 remains true because `artworkUploadApi`, `messagingApis`, and `invoiceApis` keep their current default exports and method names.
  artifacts:
    - path: FE/artium-web/src/@shared/services/apiClient.ts
      provides: Shared upload helper and upload error extraction.
    - path: FE/artium-web/src/@shared/apis/artworkUploadApi.ts
      provides: Artwork/avatar upload definitions using shared upload transport.
    - path: FE/artium-web/src/@shared/apis/messagingApis.ts
      provides: Messaging upload definition using shared upload transport.
    - path: FE/artium-web/src/@shared/apis/invoiceMocks.ts
      provides: Isolated mock invoice implementation.
---

<objective>
Standardize the special API cases: multipart uploads with progress and mock invoice responses.

Purpose: make upload and mock behavior first-class, documented exceptions to normal JSON API calls instead of ad hoc direct fetch/XHR branches inside API modules.
</objective>

<execution_context>
@.codex/get-shit-done/workflows/execute-plan.md
@.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-CONTEXT.md
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-RESEARCH.md
@.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-PATTERNS.md
@FE/artium-web/src/@shared/services/apiClient.ts
@FE/artium-web/src/@shared/apis/artworkUploadApi.ts
@FE/artium-web/src/@shared/apis/messagingApis.ts
@FE/artium-web/src/@shared/apis/invoiceApis.ts
@FE/artium-web/src/@shared/types/artwork.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a shared upload helper with progress and abort support</name>
  <read_first>
    - FE/artium-web/src/@shared/services/apiClient.ts
    - FE/artium-web/src/@shared/apis/artworkUploadApi.ts
    - FE/artium-web/src/@shared/apis/messagingApis.ts
    - FE/artium-web/src/@shared/types/artwork.ts
  </read_first>
  <files>FE/artium-web/src/@shared/services/apiClient.ts</files>
  <action>Add exported upload support to `apiClient.ts`: `ApiUploadProgress`, `ApiUploadOptions`, `ApiUploadError`, and `apiUpload<T>(path: string, formData: FormData, options?: ApiUploadOptions): Promise<T>`. `ApiUploadOptions` must support `baseUrl?: string`, `auth?: boolean`, `timeoutMs?: number`, `signal?: AbortSignal`, `onProgress?: (progress: ApiUploadProgress) => void`, and `headers?: HeadersInit`. Implement `apiUpload` with `XMLHttpRequest`, set method `POST`, use `buildApiUrl(path, baseUrl)`, attach `Authorization` when `auth !== false` and an access token exists, do not set `Content-Type` for `FormData`, parse JSON responses when content type includes `application/json`, reject with `ApiUploadError` containing `type`, `statusCode`, and `details`, and support timeout, network error, and abort handling.</action>
  <verify>
    <automated>rg -n "export type ApiUploadProgress|export type ApiUploadOptions|export type ApiUploadError|export const apiUpload|new XMLHttpRequest|xhr.upload.addEventListener|Authorization|timeoutMs|AbortSignal" FE/artium-web/src/@shared/services/apiClient.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apiClient.ts` contains `export type ApiUploadProgress`.
    - `apiClient.ts` contains `export type ApiUploadOptions`.
    - `apiClient.ts` contains `export type ApiUploadError`.
    - `apiClient.ts` contains `export const apiUpload`.
    - `apiClient.ts` contains `new XMLHttpRequest`.
    - `apiClient.ts` contains `xhr.upload.addEventListener`.
    - `apiClient.ts` contains `AbortSignal`.
    - `apiClient.ts` contains `timeoutMs`.
    - `apiClient.ts` does not set `Content-Type` inside `apiUpload`.
  </acceptance_criteria>
  <done>Shared upload transport exists with auth, progress, timeout, abort, and typed error behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate artwork and messaging uploads to shared upload transport</name>
  <read_first>
    - FE/artium-web/src/@shared/services/apiClient.ts
    - FE/artium-web/src/@shared/apis/artworkUploadApi.ts
    - FE/artium-web/src/@shared/apis/messagingApis.ts
    - FE/artium-web/src/@shared/types/artwork.ts
  </read_first>
  <files>FE/artium-web/src/@shared/apis/artworkUploadApi.ts, FE/artium-web/src/@shared/apis/messagingApis.ts</files>
  <action>Update `artworkUploadApi.ts` to use `apiUpload` instead of its local `uploadWithProgress` implementation while preserving exported functions `uploadArtworkImage`, `uploadArtworkImages`, `uploadAvatar`, default export `artworkUploadApi`, file validation rules, max file size, allowed image types, and request/response types. Update `messagingApis.uploadFile` to use `apiUpload` with `path: '/messaging/upload'`, `baseUrl: MESSAGING_BASE_URL`, and default auth behavior. Remove direct `fetch` from `messagingApis.ts` upload code and remove local token/header construction there.</action>
  <verify>
    <automated>rg -n "apiUpload|uploadArtworkImage|uploadArtworkImages|uploadAvatar|uploadFile" FE/artium-web/src/@shared/apis/artworkUploadApi.ts FE/artium-web/src/@shared/apis/messagingApis.ts</automated>
    <automated>! rg -n "new XMLHttpRequest|xhr.upload.addEventListener|fetch\\(`\\$\\{MESSAGING_BASE_URL\\}/messaging/upload|useAuthStore" FE/artium-web/src/@shared/apis/artworkUploadApi.ts FE/artium-web/src/@shared/apis/messagingApis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `artworkUploadApi.ts` contains `apiUpload`.
    - `messagingApis.ts` contains `apiUpload`.
    - `artworkUploadApi.ts` contains `uploadArtworkImage`.
    - `artworkUploadApi.ts` contains `uploadArtworkImages`.
    - `artworkUploadApi.ts` contains `uploadAvatar`.
    - `messagingApis.ts` contains `uploadFile`.
    - `artworkUploadApi.ts` does not contain `new XMLHttpRequest`.
    - `messagingApis.ts` does not contain `fetch(`.
    - `messagingApis.ts` does not import `useAuthStore`.
  </acceptance_criteria>
  <done>Upload API modules share the same transport without losing progress or file validation behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Isolate invoice mock behavior from production endpoint definitions</name>
  <read_first>
    - FE/artium-web/src/@shared/apis/invoiceApis.ts
    - .planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-CONTEXT.md
  </read_first>
  <files>FE/artium-web/src/@shared/apis/invoiceApis.ts, FE/artium-web/src/@shared/apis/invoiceMocks.ts</files>
  <action>Create `FE/artium-web/src/@shared/apis/invoiceMocks.ts` exporting `mockCreateQuickSellInvoice`, `mockGetInvoiceByCode`, `mockDeleteInvoice`, `mockUpdateInvoice`, `mockSendQuickSellInvoice`, and `mockCreateQuickSellPaymentIntent`. Move mock delay and mock response generation from `invoiceApis.ts` into this file. Update `invoiceApis.ts` to import those mock functions and keep `const USE_MOCK_API = process.env.NEXT_PUBLIC_USE_MOCK_API === 'true'`. Production branches must stay as endpoint definitions using `apiFetch` or `apiPost`. Remove mock `console.log` calls or guard them behind `process.env.NODE_ENV === 'development'` in `invoiceMocks.ts`.</action>
  <verify>
    <automated>rg -n "mockCreateQuickSellInvoice|mockGetInvoiceByCode|mockDeleteInvoice|mockUpdateInvoice|mockSendQuickSellInvoice|mockCreateQuickSellPaymentIntent" FE/artium-web/src/@shared/apis/invoiceApis.ts FE/artium-web/src/@shared/apis/invoiceMocks.ts</automated>
    <automated>! rg -n "console\\.log\\('\\[Mock API\\]" FE/artium-web/src/@shared/apis/invoiceApis.ts</automated>
  </verify>
  <acceptance_criteria>
    - `invoiceMocks.ts` contains `export const mockCreateQuickSellInvoice`.
    - `invoiceMocks.ts` contains `export const mockGetInvoiceByCode`.
    - `invoiceMocks.ts` contains `export const mockDeleteInvoice`.
    - `invoiceMocks.ts` contains `export const mockUpdateInvoice`.
    - `invoiceMocks.ts` contains `export const mockSendQuickSellInvoice`.
    - `invoiceMocks.ts` contains `export const mockCreateQuickSellPaymentIntent`.
    - `invoiceApis.ts` contains `NEXT_PUBLIC_USE_MOCK_API`.
    - `invoiceApis.ts` imports from `./invoiceMocks`.
    - `invoiceApis.ts` does not contain `[Mock API]`.
  </acceptance_criteria>
  <done>Invoice mock behavior is isolated and production endpoint definitions are easier to audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local files -> remote upload endpoint | Upload code handles user-controlled files and must keep auth, timeout, and abort behavior predictable. |
| Mock mode -> production API branches | Mock-only code must not obscure or alter production endpoint behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Information Disclosure | upload auth headers | mitigate | Centralize upload auth and avoid ad hoc token handling in modules. |
| T-27-08 | Denial of Service | upload requests | mitigate | Support abort and timeout consistently in shared upload helper. |
| T-27-09 | Tampering | invoice mock mode | mitigate | Isolate mock behavior and keep production endpoint definitions explicit. |
</threat_model>

<verification>
- Automated: `cd FE/artium-web && npm run lint`
- Automated: `cd FE/artium-web && npm run build`
- Structural: `rg -n "apiUpload|ApiUploadOptions|ApiUploadProgress|invoiceMocks" FE/artium-web/src/@shared/services/apiClient.ts FE/artium-web/src/@shared/apis`
</verification>

<success_criteria>
- Artwork/avatar uploads and messaging uploads use a shared upload helper.
- Upload progress, abort, timeout, auth, and error behavior remain supported.
- Invoice mock functions live outside the production invoice API module.
- Current API module default exports remain available.
</success_criteria>

<output>
After completion, create `.planning/phases/27-frontend-shared-api-definition-standardization-and-edge-case/27-03-SUMMARY.md`.
</output>
