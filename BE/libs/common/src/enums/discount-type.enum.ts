/**
 * Discount calculation method
 * Used in: Promotion.discountType
 */
export enum DiscountType {
  /** Percentage discount (e.g., 10% off) */
  PERCENT = 'percent',
  /** Fixed amount discount (e.g., $50 off) */
  FIXED = 'fixed',
}
