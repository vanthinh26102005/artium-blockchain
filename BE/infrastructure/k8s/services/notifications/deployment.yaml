apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
  namespace: dev
  labels:
    app: notifications-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
    spec:
      containers:
        - name: notifications-service
          image: dgpthinh/notifications-service:v1
          command: ["yarn", "run", "dev:notifications"]
          ports:
            - containerPort: 3002
          readinessProbe:
            tcpSocket:
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3002
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            limits:
              cpu: "300m"
              memory: "384Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
          volumeMounts:
            - name: notifications-service
              mountPath: /app
            - name: node-modules
              mountPath: /app/node_modules
      volumes:
        - name: notifications-service
          emptyDir: {}
        - name: notifications-node-modules
          emptyDir: {}